# Claude Code

Read **`AGENTS.md`** — the single source of truth for all AI instructions on this project.

- Instruction files: **`.ai/`**
- Command recipes: **`.ai/commands/`** (macro + micro)
- Local tool permissions only: **`.claude/settings.local.json`**
