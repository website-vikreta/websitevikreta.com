# Learning Log — Uniformity & Consistency

Persistent memory of design + code conventions for this site. Every reusable decision lands here so the site stays uniform across pages and sessions.

## How To Use This File
- **Before building** any page/component: read this file. Reuse what exists. Do not reinvent.
- **After learning** anything reusable: log it here immediately. One entry = one rule.
- **On conflict**: if new work contradicts a logged rule, stop. Either follow the rule or update the rule with reason. Never silently diverge.
- Keep entries short, specific, copy-pasteable. Prefer exact values (px, ms, hex, token names) over prose.

## Entry Format
```
### [Topic] — short title
- Rule: <the convention, exact values>
- Where: <files/components using it>
- Why: <reason, if not obvious>
- Date: YYYY-MM-DD
```

---

## Spacing & Layout
### [Section] — vertical rhythm
- Rule: Every top-level page section uses `py-16 md:py-20`. No other section padding values.
- Where: all `components/sections/*` on home page.
- Why: equal gaps between components = uniform page rhythm.
- Date: 2026-06-28

### [Section] — heading-to-content gap
- Rule: Section heading block bottom margin = `mb-10 md:mb-14`.
- Where: home page section headers.
- Date: 2026-06-28

## Typography
### [Hierarchy] — section H2
- Rule: Section headings use the `text-h2` token (`clamp(1.75rem,4vw,3rem)`, `leading-h2`) + `font-bold` + `text-(--color-text)`. Never raw `text-3xl/4xl` or custom clamps for a section heading.
- Where: all home sections (Stats, Services, Clients, Featured, Tech, Testimonials, Blog).
- Why: one section-heading size across page = consistent hierarchy.
- Date: 2026-06-28

### [Hierarchy] — card / item title (h3 tier)
- Rule: Card/list item titles use `text-2xl sm:text-3xl font-bold`. One tier below section H2.
- Where: FeaturedWork case cards, Services stack cards.
- Date: 2026-06-28

### [Hierarchy] — intentional H2 step-down for content-heavy sections
- Rule: Not every section H2 has to sit at the full `text-h2` step. For a two-col text/image section where the heading is immediately followed by dense multi-paragraph body copy (not a short punchy line), drop the heading one step to `text-h3 font-bold tracking-tight text-(--color-text)` — and drop any hand-set `leading-[1.05]` override with it, since `.text-h3` already carries the correct `--leading-h3` (1.3) for that size. Punchier sections (short heading, grid/canvas/interactive content below, not paragraph-dense) keep the full `text-h2`.
- Where: `app/services/ai-automations/sections/PainSection.tsx` and `WhySection.tsx` (both the "heading + 4/2 paragraphs + image" pattern) stepped to `text-h3`; `FixesSection.tsx` and `HowWeWork.tsx` (short heading + grid/canvas) stayed `text-h2`; Hero `h1` already sits at `text-h1` (one step below `text-display`), left alone.
- Why: user flagged that uniform full-size H2 on every section made every heading compete equally for attention regardless of role, overwhelming the page. Varying the step by section role (content-heavy vs. punchy) gives the page a real hierarchy while staying entirely on the existing type scale — no new tokens.
- Date: 2026-07-04

### [Hierarchy] — section subhead (H3 token)
- Rule: Section subheading under an H2 uses the `.text-h3` utility (`--text-h3` = `clamp(1.5rem,3vw,2.125rem)`, `--leading-h3` = 1.3) + `font-semibold` + `text-(--color-text-muted)`, spaced `mt-3 md:mt-4` below the H2. Wrap in `RevealText as="h3"` with `delay={0.12}` so it staggers after the H2 line reveal. Reuse `.text-h3` — never raw clamps/text-xl for a section subhead.
- Where: TechnologiesSection ("Powered by AI. Future Tech." → subhead).
- Why: gives section heads a two-line H2→H3 hierarchy that stays on the design-system scale.
- Date: 2026-06-29

### [Anti-pattern] — no uppercase letter-spaced eyebrow labels
- Rule: Never use `uppercase` + `tracking-wide/widest` (mono or not) as a small label above/on a heading or card. Reads as AI-generated SaaS template ("AI slop"), not human design. For meta info on cards (type, count, tags, skills), use plain sentence-case text: `text-sm text-(--color-text-muted)` (primary meta) or `text-sm text-(--color-text-faint)` (secondary), comma/`·` separated. No mono font, no caps, no wide tracking.
- Where: `app/careers/CareersClient.tsx` card meta rows (was `font-mono text-meta-label uppercase text-(--color-text-faint)`, now `text-sm text-(--color-text-muted/faint)`).
- Why: user flagged uppercase-tracked eyebrows as the single biggest tell of AI-slop design. Kills prior [Hierarchy] "eyebrow / meta label" rule below — superseded.
- Date: 2026-07-03

## Color & Accent
### [Tokens] — no raw hex for text/surface
- Rule: Always `var(--color-*)` tokens. Never raw hex (`#121212`, `#525252`, …) for text/bg/border. Accent = `var(--color-accent)` (#FFD600).
- Where: converted Testimonials hardcoded hex → tokens.
- Date: 2026-06-28

### [Reverted] — LikeButton liked state: pink → solid black/white fill toggle
- Rule: `LikeButton` (`components/blog/LikeButton.tsx`) liked state is `fill-(--color-text)` / `text-(--color-text)` — a solid heart in ink black vs. the default outline in `--color-text-muted`. No color at all, just filled vs. outline.
- History: briefly shipped as `text-pink-500`/`fill-pink-500` after the user was shown the single-accent conflict and picked pink anyway (2026-08-08) — reverted the same day when the user asked for something on-theme instead. Accent yellow (`--color-accent`) was considered and rejected: `#FFD600` text/icon on a white card fails contrast (same reason the category badge uses dark text ON a yellow chip, never yellow text on white — see design-system precedent), and coloring every liked heart across a page of cards would also blow the separately-logged "max ~1-3 accent instances per page" budget. A plain fill/outline toggle sidesteps both problems and needs no new token.
- Where: `components/blog/LikeButton.tsx`, used on the blog post detail page and every `BlogCard`.
- Date: 2026-08-08

## Components & Patterns
### [Reveal] — shared motion primitives
- Rule: Use `components/ui/Reveal.tsx` for ALL scroll reveals. Never hand-roll GSAP/motion per section.
  - `RevealText` — masked line reveal (overflow-hidden clip + inner translateY 110%→0). For headings + titles. Props: `as`, `delay`, `duration`.
  - `RevealFade` — fade-up (opacity 0 + y24 → 0). For cards + supporting text. Stagger a group with `delay={i * 0.1}`.
  - `Counter` — count 0→value once scrolled into view (power2.out).
- Easing: single curve `REVEAL_EASE = [0.16, 1, 0.3, 1]` (~expo.out, matches hero).
- Trigger: all primitives use `viewport`/`useInView` with `once: true` + `margin: '0px 0px -200px 0px'` (one shared `REVEAL_MARGIN`). Element must be ~200px inside the viewport before it fires — gives a beat instead of firing at the edge.
- `RevealText` observes the STATIC clip wrapper (not the moving inner line) — see anti-pattern below.
- Where: every home section.
- Date: 2026-06-28

## Animation & Motion
### [Home] — unified reveal language
- Rule: One motion language across the whole page (mirrors the hero overflow-hidden reveal).
  - Section headings + titles → `RevealText` masked slide-up.
  - Cards (Stats, Services stack, Featured cases, Blog) → `RevealFade` fade-up, staggered by index.
  - Stat numbers → `Counter` count-up 0→value.
- Multi-line heading: one `RevealText` per line, stagger via `delay` (e.g. Services lines 0 / 0.12).
- Card titles animate WITH their card (fade), not as separate masks. Section headings get the mask.
- Untouched (own motion): continuous marquees (Clients, Tech), Services sticky-stack scale (now also fades in), testimonial carousel switching.
- Hero stays bespoke (GSAP, load-triggered word stagger) — the reusable primitives copy its look for scroll-triggered sections.
- Why: unified style + structure across the page; consistency is the goal.
- Date: 2026-06-28 (supersedes earlier "reveals reserved for hero" rule)

## Code Conventions
### [Reuse] — shared primitives over per-component motion
- Rule: Scroll reveals come from `components/ui/Reveal.tsx`. Don't hand-roll GSAP/motion `whileInView` per section. New section = import `RevealText`/`RevealFade`/`Counter`.
- Why: one easing/timing/trigger everywhere; fix once, fixes all.
- Date: 2026-06-28

### [Hex] — tokens only in components
- Rule: No raw hex in component files (matches code-standards.md). Use `var(--color-*)`.
- Date: 2026-06-28

### [WYSIWYG] — shared PortableText renderer
- Rule: Any Sanity block-content field rendered as rich text (blog body, career "About the Internship") goes through the single `PortableTextContent` component + `ptComponents` map in `components/ui/PortableTextContent.tsx`. Don't re-declare `ptComponents` per page. Import and pass `value={blocks}`.
- Where: used by `app/blog/[slug]/page.tsx` and `app/careers/[slug]/CareerDetailClient.tsx`.
- Why: one heading/list/mark/image/table style everywhere rich text appears — matches [Reuse] rule above; extracted 2026-07-03 when careers needed the same renderer for a new block-content field.
- Date: 2026-07-03

### [Card] — bordered-grid listing card (openings / roster type lists)
- Rule: For grid listings of clickable items (e.g. job openings), don't use individually-boxed cards with `bg-(--color-bg-muted)` fill + per-tag chip borders + a `border-t` divider before the footer. Instead: one shared `border-t border-l border-(--color-border)` wrapper around the grid, each cell gets `border-r border-b border-(--color-border)` (put these on the `RevealFade` wrapper, not the inner `<Link>`), and the whole cell is a single `<Link>` with `bg-(--color-surface)` (not `--color-bg-muted`) and `hover:bg-(--color-bg-muted)` for feedback. Type/count meta row stays plain sentence-case `text-sm text-(--color-text-muted)` text, never uppercase/tracked (see [Anti-pattern] no eyebrow labels). Skills render as pills: `rounded-full border border-(--color-border) px-3 py-1 text-sm text-(--color-text)`, wrapped `flex flex-wrap gap-2` (user-requested 2026-07-03 — supersedes earlier "never bordered chips" for skills specifically; type/count row stays plain text). Tried `bg-(--color-accent)/10` fill first — on `--color-surface: #FFFFFF` the 10%-opacity yellow tint was too pale to read as a pill boundary; hairline border reads clean instead. Don't retry faint accent fills for pill/chip backgrounds on white surface. No internal `border-t` separator — `mt-auto` + gap spacing pushes the footer down. Title `text-2xl font-bold` per design-system.md card-title tier (earlier `text-lg` was too weak — user flagged card titles as not prominent). Stipend kept quiet: `text-sm font-normal text-(--color-text-muted)`, not emphasized.
- Where: `app/careers/CareersClient.tsx` openings grid.
- Why: individually-boxed muted cards + chip borders + internal divider read as dark/cluttered. The bordered-grid pattern already exists in `FeaturedWorkSection` — reusing it keeps listing UIs uniform and reads minimal (one hairline border language, not stacked boxes). NOTE (2026-07-03): tried replacing this with a numbered full-width editorial list — user rejected it as harder to understand; the simple boxed grid is the approved direction. Don't re-propose the list.
- Date: 2026-07-03

### [Card] — truncated skills pill row with stacked "+N" overflow badge
- Rule: Skill pill rows on cards cap at 2 visual lines. Use `components/ui/SkillsPills.tsx` (client component) instead of mapping the skills array directly — it measures the rendered `offsetTop` of each pill after layout, drops pills one at a time until only 2 rows remain, then appends an overflow badge: two stacked pill shapes (a decorative `absolute inset-0 translate-x-1 translate-y-1` pill behind, `z-10` real pill in front), front pill text is number-only (`+{N}`, no "skill(s)" word). Re-measures on window resize and when the `skills` prop changes (grid column count changes the wrap point per breakpoint). Don't hardcode a fixed pill count (e.g. "show first 3") — actual wrap point varies by pill text length and card width.
- Where: `app/careers/CareersClient.tsx` (replaced the inline `.map` over `opening.skills`).
- Why: user wanted overflow truncated at 2 lines with a "stack of pills" look, count-only label — no "skill"/"skills" word.
- Date: 2026-07-03

### [Page] — page-level heading hierarchy + a11y (every page)
- Rule: Every page has exactly ONE `<h1>` (the hero/page title, `text-h1`), section headings are `<h2>` (`text-h2`), card/item titles are `<h3>`. Never skip levels, never two `<h1>`, never use `<h2>` for the hero while a section also uses `<h2>`. Match the visual size to the semantic level (h1 > h2 > h3). Interactive cards/links get a keyboard focus state — `focus-visible:outline-2 focus-visible:-outline-offset-2 focus-visible:outline-(--color-text)` on the card `<Link>`, and drive any hover-only affordance (e.g. underline-draw) with `group-focus-visible:` too so keyboard users see the same feedback as hover.
- Where: `app/careers/CareersClient.tsx` (Careers=h1, Open Roles=h2, card titles=h3).
- Why: careers page originally had two `<h2>` and no `<h1>` (broken hierarchy + a11y fail) and hover-only affordances with no keyboard focus. Screen-reader/keyboard users need one h1, ordered levels, and visible focus.
- Date: 2026-07-03

### [Reveal] — no hand-rolled motion variants on pages
- Rule: Don't define local `container`/`lineReveal`/`fadeUp` motion variants inside a page/section and wire them to `motion.h2`/`motion.p`. Use `RevealText` (headings) + `RevealFade` (supporting text, cards) from `components/ui/Reveal.tsx`. Also: no arbitrary inline `style={{ fontSize }}` — use type tokens (`text-h1/h2/h3`, `text-body-lg`, `text-base`, `text-meta-label`). And keep token syntax consistent: use the `text-(--color-x)` shorthand everywhere, not a mix of `text-[var(--color-x)]` and `text-(--color-x)`.
- Where: `app/careers/CareersClient.tsx` (removed local variants → RevealText/RevealFade; removed inline fontSize; unified two card meta lines to `text-meta-label`).
- Why: hand-rolled variants + arbitrary inline sizes + mixed token syntax are the "AI slop" tells — they diverge from the site's one motion/type system. Reuse the primitives; the page then reads as part of the whole.
- Date: 2026-07-03

### [Anti-pattern] — no linework geometry / registration marks on hero backgrounds
- Rule: Don't add decorative background shapes (outline circles, rotated squares, crosshair "registration marks") to hero sections to fill negative space. Tried on homepage hero and careers hero — user rejected as poor design both times. Don't re-propose this direction; if a hero reads empty, revisit brainstorm (grain/texture, ghost type, vignette) rather than defaulting back to linework shapes.
- Where: tried in `components/sections/HeroSection.tsx` and `app/careers/CareersClient.tsx` hero, reverted both.
- Date: 2026-07-03

### [Badge] — corner flag indicator (opening cards)
- Rule: Sanity `opening.flag` (dropdown: `New` / `Hiring Urgently`) renders as `absolute top-0 right-0 px-3 py-1 text-xs font-medium uppercase tracking-wide` on the card `Link` (needs `relative` added to that Link). Color per value via a `FLAG_STYLES` map: `New` → `bg-(--color-accent) text-(--color-text)` (brand yellow), `Hiring Urgently` → `bg-[#FF4444] text-white` (reuses the existing form-validation red token, not a new hex). Field is optional — no flag renders no badge.
- Where: `app/careers/CareersClient.tsx`.
- Why: user wanted per-category color-coded corner indicators; brand.md caps yellow to one accent, so urgency red reuses the color already established for error states elsewhere in the codebase rather than introducing a second new hue.
- Date: 2026-07-03

### [Content] — opening short vs. full description
- Rule: `opening.shortDescription` (renamed from `description`) is plain text — used on the careers card and directly below the title on the detail page. `opening.description` is now a block-content array rendered via `PortableTextContent` under an "About the Internship" heading, positioned above Prerequisites on the detail page. Don't conflate the two — short stays plain text for card/meta use, long is rich text for the body.
- Where: `sanity/schemaTypes/openingType.ts`, `app/careers/[slug]/CareerDetailClient.tsx`.
- Date: 2026-07-03

### [Page] — career detail page: h1/h2 hierarchy + stipend/positions line + form card
- Rule: On `app/careers/[slug]/CareerDetailClient.tsx`: page title (`opening.title`) is `<h1>` styled `text-h2` (matches sibling detail page sizing — blog post title, careers listing hero). Every sub-block heading ("About the Internship", "Prerequisites", "Skills You Will Work With", "Apply now!") is `<h2>` styled `text-h3 font-bold tracking-tight text-(--color-text) mb-6` (shared `SUBHEAD` const in the file) — never `text-lg`/ad-hoc sizes for these. Stipend + position count render as ONE quiet line (`₹{stipend} / month · {positions} {position(s)} available`, `text-sm text-(--color-text-muted)`, `mt-4`) directly under the short description — not two separate `<p>` blocks with their own margins. Skills pills reuse the rounded-pill spec from the [Card] bordered-grid entry (`rounded-full border border-(--color-border) px-3 py-1 text-sm text-(--color-text)`), not the old mono/hover-invert chip style. Form field labels are plain sentence-case (`text-sm font-medium text-(--color-text-muted)`) — no uppercase/tracking (see [Anti-pattern] no eyebrow labels; form labels count too, they hurt readability same as card eyebrows). The application form column gets a white card treatment: `bg-(--color-surface) border border-(--color-border) p-6 md:p-10` wrapping the whole right column div.
- Where: `app/careers/[slug]/CareerDetailClient.tsx`.
- Why: page previously had two `<h2>` (title + "Apply now!") and no `<h1>`, sub-headings used arbitrary `text-lg`, stipend/positions were two loosely-spaced paragraphs, skills used mono hover-invert chips inconsistent with the listing page, form labels were uppercase-tracked (flagged anti-pattern), and the form had no surface treatment distinguishing it from the page background.
- Date: 2026-07-03

### [Card] — equal 3-up image/title/description/cta grid
- Rule: For a simple 3-item feature grid, use the flat bordered-grid wrapper (`border-t border-l` on `grid grid-cols-1 md:grid-cols-3`, each cell `border-r border-b border-(--color-border)`) — same pattern as the careers openings grid, equal-size cells, nothing asymmetric. Cell: `aspect-video` image on top (`object-cover`, `group-hover:scale-105` on the image only for a quiet hover-zoom), then `h3` title (`text-2xl font-bold`), description, `Button variant="ghost" size="sm" showArrow` CTA. A one-off numeric proof point (e.g. "20h → 1h") renders as a small dark pill (`bg-(--color-text) text-(--color-bg)`, strikethrough-from / accent-arrow / bold-to) absolutely positioned over the image corner — not a large separate display-type block — so it doesn't break the equal-height row rhythm.
- Where: `app/services/ai-automations/AIAutomationsClient.tsx` "Three Things We Fix Most Often" section.
- Why: first pass used an asymmetric bento (one cell `md:row-span-2` as a "hero" cell). User rejected it as "too hard to interpret" — a flat equal grid reads faster; save asymmetric/bento treatments for when content genuinely differs in weight, not as a default "creative" move. That first pass also had 01/02/03 index labels — cut, see [Anti-pattern] no numbered card index below.
- Date: 2026-07-04

### [Consistency pass] — AI Automations page spacing/dead-code normalization
- Rule: On a "full-width heading block, then unrelated content (grid/canvas)" pattern (§3 FixesSection, §4 HowWeWork), the heading wrapper bottom margin is `mb-10 md:mb-14` — matches the design-system.md heading→content token exactly. Post-refactor these had drifted to `mb-10 md:mb-12` (Fixes) and `mb-12 md:mb-14` (HowWeWork); normalized both to `mb-10 md:mb-14`. The other shared pattern (two-col text/image: PainSection, WhySection) already used `mb-6` on the h2 directly followed by paragraphs — that's a different structural pattern (heading inline with body copy, not a standalone heading block) and was already consistent between the two, left as-is.
- Where: `app/services/ai-automations/sections/FixesSection.tsx`, `app/services/ai-automations/sections/HowWeWork.tsx`.
- Why: 6 independent agents built each section in isolation; this pass reconciled the drift back to the logged design-system.md token.
- Date: 2026-07-04

### [Cross-page pass] — AI Automations Hero/ContactSection padding matched to sitewide hero/CTA exception
- Rule: Confirmed `py-16 md:py-20` (PainSection, FixesSection, HowWeWork, WhySection) already matches the real sitewide body-section standard — verified against `components/sections/TechnologiesSection.tsx`, `ServicesBentoGrid.tsx`, `ClientLogosSection.tsx`, `FeaturedWorkSection.tsx`, `TestimonialsSection.tsx`, `StatsCounters.tsx` (all `py-16 md:py-20`), so no change needed there. For the two full-bleed exception sections: `Hero.tsx`'s container padding was `pt-28 pb-20 md:pt-32 md:pb-24` (missing the `lg:` step) — added `lg:pt-36 lg:pb-28` to match home's `HeroSection.tsx` (`pt-28 pb-20 md:pt-32 md:pb-24 lg:pt-36 lg:pb-28`) exactly, since both are `min-h-svh` GSAP-choreographed heroes. `ContactSection.tsx` (closing contact-form section) was `pt-28 md:pt-36 pb-20 md:pb-28` — changed to `pt-32 pb-24 md:pt-40 md:pb-32` to match the one real sitewide precedent for a large-padding contact/form-focused section: `components/ContactPageContent.tsx`'s single section (`pt-32 pb-24 md:pt-40 md:pb-32`).
- Where: `app/services/ai-automations/sections/Hero.tsx`, `app/services/ai-automations/sections/ContactSection.tsx`.
- Discrepancy vs learning.md/design-system.md: both say flatly "every top-level section uses py-16 md:py-20, no other section padding" with no carved-out hero/CTA exception — but real code consistently diverges for hero-type and contact/form-focused sections (home Hero, careers page-title header, standalone /contact page section all use larger, non-py-16/20 padding). Trusted the actual code pattern per instruction; the docs should arguably note this exception explicitly but weren't touched since editing standards files wasn't in scope for this task. One home-page outlier (`BlogPreviewSectionClient.tsx`, `py-20 md:py-28`) was treated as unintentional drift, not a pattern, since it's a single instance against 6+ consistent `py-16 md:py-20` sections — not adopted anywhere.
- Why: user wanted AI Automations' section rhythm to match the established cross-page convention, not just be internally consistent.
- Date: 2026-07-04

### [Consistency pass] — ContactSection typography tokenized, FixesSection card-title responsive step restored
- Rule: `ContactSection.tsx` had drifted into ad-hoc inline `fontSize`/clamp styles (form-card heading, success message, sub paragraph, 5 field-error/alert texts) instead of the design-system scale — same "AI slop" tell logged for careers. Fixed: sub paragraph → `text-body-lg leading-relaxed` (matches Pain/Why/Hero pattern); form-card heading "Book a Free Process Audit" → `text-2xl sm:text-3xl font-bold leading-[1.1]` (the logged H3-tier card-title convention, was missing the `sm:` step); success message → `text-h3 font-normal` (color/max-width kept as inline override, those aren't typography-scale concerns); 5 error/alert texts unified to `text-xs` (were 0.75rem/0.8125rem inline, `color`+`fontFamily: monospace` kept inline since `font-mono` utility is remapped to Utile sans in this project, not a real monospace). Also fixed a real bug found in the same file: `className="pt16 ..."` (missing dash) silently dropped the top-padding entirely. Separately, `FixesSection.tsx` card `<h3>` was `text-2xl` only — added the missing `sm:text-3xl` step to match the logged H3-tier convention.
- Where: `app/services/ai-automations/sections/ContactSection.tsx`, `app/services/ai-automations/sections/FixesSection.tsx`.
- Why: user asked for a full responsive-typography audit across all 6 ai-automations sections; the fluid `clamp()` tokens already cover cross-device scaling, so the actual defects were inline styles bypassing the token system (harder to keep consistent) and one missing responsive breakpoint, not the scale itself.
- Date: 2026-07-04

### [Motion] — RevealImage: editorial clip-wipe image reveal, motion/react port of AI-Automations' `revealClipImage`
- Rule: `RevealImage` (`components/ui/Reveal.tsx`) is the canonical primitive for image reveals sitewide — clip-path wipes `inset(0 0 100% 0)` → `inset(0 0 0% 0)` on the outer container (same `duration: 1.0, ease: REVEAL_EASE` as `RevealText`), inner wrapper scales `1.08 → 1` over `1.4s` (matches gsap `DUR.crawl`). Pass the image container's own `overflow-hidden`/border/radius classes as `className`; children is the `<Image fill />`. This was previously GSAP-only (`lib/gsap/reveals.ts` `revealClipImage`, used on AI-Automations page); ported to motion/react so non-GSAP pages (About, and any future page using the `Reveal.tsx` primitives) get the same image-reveal language as text reveal already had. Layer it with `RevealFade` on a wrapping grid/card element for entrance-stagger + independent image wipe — same two-trigger layering as `FixesSection.tsx` (`.fix-card` fades, `.fix-card-image` wipes separately).
- Where: `components/ui/Reveal.tsx` (`RevealImage`), used in `components/sections/AboutHeroSection.tsx` (hero image) and `components/sections/PhotoGallerySection.tsx` (all gallery images, including both id=4 side-by-side images).
- Why: user asked for the About page to carry the same text + image revealing animations as the AI-Automations page. Text reveal already matched (`RevealText`/`RevealFade` already mirror `revealLines`/`revealFadeUp`); the image clip-wipe was the missing piece — About page images previously only got a plain fade+rise via `RevealFade`, no clip-path/scale treatment.
- Date: 2026-07-31

### [Hero] — typographic "before → after" hero (About page)
- Rule: When a page's story is a pivot/change, the hero headline should *enact* the change with type, not describe it with an image beside it. Pattern: line 1 = the dead model at `text-h1 font-normal` in `text-(--color-text-faint)` with the key noun struck through by an animated bar (`absolute left-0 top-[0.56em] h-[0.045em] w-full origin-left bg-(--color-text)`, `motion.span` `scaleX 0→1`, `duration 0.55`, `REVEAL_EASE`, `delay 0.75` so it lands after the line reveal, `aria-hidden`); line 2 = the live model at full `text-display font-bold` in `text-(--color-text)` with the key phrase in `text-(--color-accent)`. Put the weight on the individual lines, NOT `font-bold` on the `<h1>` — the size jump (h1 → display) AND the weight jump (normal → bold) both carry the pivot; don't set both lines at the same step or the same weight. Strike bar stays near-black so yellow remains the single accent moment on the page. Guard with `useReducedMotion()` from motion/react → `initial={{ scaleX: reduced ? 1 : 0 }}` and `delay: reduced ? 0 : 0.75` so the mark is present without animating.
- Strike bar vertical position: use `top-[0.56em]`, never `top-1/2`. `top-1/2` centres on the line box, which lands the bar visibly above the lowercase letters and reads like an underscore floating in the wrong place; `0.56em` sits on the lowercase x-height centre. Bar thickness `0.045em` — matches a `font-normal` stroke; `0.06em` reads too heavy against light-weight type. Both are `em` units so they scale with the fluid `--text-h1` clamp.
- Where: `components/sections/AboutHeroSection.tsx`.
- Why: first pass was headline-left/photo-right at a 5/7 split — user rejected it as "very generic, very hand made, nothing creative". A column-ratio change isn't a design idea; the story (agency killed its old model) had to become the visual. Accent-word-in-display-type matches the existing homepage hero precedent (`HeroSection.tsx` `ACCENT_INDEX` colors one word `--color-accent` at `text-display` on the same `#FAFAF7` bg), so yellow display text is an established pattern here, not a new one.
- Date: 2026-07-31

### [Hero] — no photo in a hero when the page already has a photo gallery
- Rule: Don't put a team/office photo in a page hero when a `PhotoGallerySection` (or equivalent) further down already carries that content. The hero goes pure-type at `min-h-svh` with aggressive whitespace instead. Also check `.claude/context/brand.md` photography direction before adding any hero photo — "no stock photo smiles, no handshakes, no laptops on desks" rules out most meeting/workspace shots as a hero anchor.
- Where: `components/sections/AboutHeroSection.tsx` (dropped `/about/team-strategy-meeting.webp`; the About page's 8 other team photos still run in `PhotoGallerySection`).
- Why: the hero photo was the 9th team photo on the page and the first one the user saw, so it set a generic "agency stock" tone before the gallery got its moment. Removing it also gives the headline the full viewport width the brand's typography direction asks for ("Large. Bold. Words fill the screen.").
- Date: 2026-07-31

### [Carousel] — infinite auto-scroll marquee with arrow controls
- Rule: For a card carousel that needs continuous auto-scroll + manual prev/next (not the switching single-quote pattern in `TestimonialsSection.tsx`), reuse `ClientLogosSection.tsx`'s motion primitives: `useMotionValue` + `useAnimationFrame` moving `x` left at a constant px/s, list duplicated 2x, wrap at `-trackWidth/2`. Add prev/next by animating the same motion value with `animate(x, target, { duration: 0.6, ease: REVEAL_EASE })` stepping by one card width + gap, and pause the auto-scroll frame loop (`isPaused` ref) on hover and for 4s after a manual arrow click so the click doesn't get immediately overridden. Same edge-fade masks (`bg-gradient-to-r/l from-(--color-bg) to-transparent`) as the client-logo marquee. Card: `bg-(--color-surface) border border-(--color-border) hover:border-(--color-border-strong)`, no shadow (Card rule). Label reading "Testimonial"/category tag on a card is plain `text-sm text-(--color-text-faint)`, never uppercase/tracked (see [Anti-pattern] no eyebrow labels — applies to any small card label, not just careers meta).
- Where: `components/sections/work/WorkTestimonialsSection.tsx` (Work page only — home page keeps the original switching `TestimonialsSection.tsx`).
- Why: user wanted a 6-card testimonial carousel (name/designation/company/photo) with infinite auto-scroll + explicit left/right controls, which the existing single-quote switcher doesn't provide. Reusing the marquee math instead of a new scroll-snap/library carousel keeps the site to one continuous-scroll motion primitive.
- Update (2026-08-01) — touch + mobile corrections to the above:
  - **Card width is never a fixed px constant.** `CARD_W = 380` overflowed the viewport on any phone under 380px. Use `w-[80vw] max-w-[380px] sm:w-[380px]` and *measure* the arrow step (`cardRef.current.offsetWidth + GAP`) into state on mount and on resize — a viewport-relative card makes the step unknowable at author time.
  - **Add `drag="x"` (`dragMomentum={false} dragElastic={0}`) to the track.** Hover-pause is the only pacing control a mouse user needs, but touch never fires hover — swipe is the equivalent. `onDragStart` pauses, `onDragEnd` re-wraps `x` and resumes after `RESUME_DELAY`. Give the avatar `<Image draggable={false} className="select-none">` so native image-drag doesn't hijack the gesture.
  - **Wrap continuously, except during an arrow tween.** An `isTweening` ref makes the frame loop skip wrapping while `animate()` owns `x` (wrapping mid-tween yanks the value back toward a stale target); the tween's `onComplete` re-wraps instead. Everything else — drag included — wraps every frame, so no gesture can drag the track past the duplicated copy into empty space.
  - **Guard with `useReducedMotion()`** — auto-scroll velocity goes to 0 and the arrow tween duration to 0; the arrows keep working.
  - Import `REVEAL_EASE` from `Reveal.tsx`; don't re-declare `[0.16, 1, 0.3, 1]` inline. Same correction applied to `FaqSection.tsx`, which had drifted to its own `EASE = [0.22, 1, 0.36, 1]` at `duration: 0.35` — now `REVEAL_EASE` at `0.7` / `y: 24`, matching `RevealFade` exactly.
- Date: 2026-08-01

### [Component] — shared FaqSection for page-specific FAQ subsets
- Rule: Page-level FAQ blocks (a curated subset of questions relevant to that page, not the full list) use `components/sections/FaqSection.tsx` — `<FaqSection items={someFaqs} viewAllHref="/faq" />`. Don't hand-roll the accordion per page (was duplicated between `app/faq/FaqPageContent.tsx` and `app/services/web-development/WebDevClient.tsx` before this). Heading row is `flex items-end justify-between`: `RevealText` H2 on the left, a "Read all FAQs" arrow-link (`ArrowUpRight`, same hover-lift as the bottom "Get in touch" link) on the right pointing at `viewAllHref` — this is the piece that makes it a page-specific excerpt rather than the full page. All FAQ content (question/answer/id) lives in one place: `lib/faq-data.ts` (`ALL_FAQS`, `FaqItem` type) — the standalone `/faq` page imports `ALL_FAQS` directly (keeps its own bespoke GSAP header + numbered `<h2>`-per-item layout, that page is intentionally NOT `FaqSection` since it's the "view all" destination, not an excerpt), other pages import `ALL_FAQS` and `.filter()` to a relevant subset (or define their own local array typed `FaqItem[]` if the copy is page-specific, e.g. `webDevFaqs` in `WebDevClient.tsx`) and pass it to `FaqSection`.
- Where: `components/sections/FaqSection.tsx`, `lib/faq-data.ts`; consumed by `lib/work-data.ts` (`WORK_FAQS`, filtered subset of `ALL_FAQS` by id) → `app/work/WorkPageContent.tsx`, and `app/services/web-development/WebDevClient.tsx` (local `webDevFaqs`, replaced its inline duplicate accordion).
- Why: user asked to reuse the FAQ pattern for the Work page with a "read all" link back to `/faq` — at that point there were two independent hand-rolled copies of the same accordion (full FAQ page + WebDev page), so extracting a shared component now instead of writing a third copy keeps the pattern from drifting further (same reasoning as the earlier PortableText/Reveal-primitive extractions logged above).
- Date: 2026-08-01

### [Hero] — proof band instead of decorative filler
- Rule: If a full-viewport type hero reads empty at the bottom, close it with a hairline-anchored proof band (`border-t border-(--color-border) pt-6`, flex-wrapped `<ul>`, items `text-sm text-(--color-text-muted)`, `gap-x-10 gap-y-2`) carrying real, already-published numbers — never invented figures, and never decorative shapes (see [Anti-pattern] no linework geometry). Source the numbers from an existing section on the same page so they can't drift.
- Where: `components/sections/AboutHeroSection.tsx` — "Pune, India / Five years in / 68 projects shipped / 6,360 hours given back", all sourced from `StatsCounters.tsx`'s `STATS` array.
- Date: 2026-07-31

### [Numerals] — big-number treatment (one style sitewide)
- Rule: Any large numeral used as a design element — stat values, numbered principle/step lists — uses one treatment: `font-mono font-bold leading-none` + `text-5xl md:text-6xl` + `tracking-[-0.05em]`. Colour is `text-(--color-text)` for factual stats and `text-(--color-accent)` for numbered list indices. Never `uppercase` on digits, and never `tracking-(--tracking-meta)` (0.12em) — wide meta tracking is for label text and visibly pulls digits apart. Note `font-mono` is remapped to Utile sans in this project (not a true monospace) — it's kept because `StatsCounters` established it as the numeral face.
- Where: `components/sections/StatsCounters.tsx` (values, `text-(--color-text)`), `components/sections/CoreValuesSection.tsx` (01–06 indices, `text-(--color-border-strong)` ghost → `--color-accent` on `group-hover`).
- Why: user asked for the CoreValues 01–06 indices to read like "The Numbers So Far" but in yellow, with the digits tightened. They were `text-xl sm:text-2xl uppercase tracking-(--tracking-meta)` — a different size step, a different weight feel, and letter-spacing pulling the two digits apart.
- Open item: `StatsCounters` itself still has no explicit tracking (browser default 0). If the tightened digits read better there too, add `tracking-[-0.05em]` to its value span so both fully match — not done yet, wasn't requested.
- Date: 2026-07-31

### [Exception] — numbered indices ARE allowed on a principles list (user-confirmed twice)
- Rule: The "never number cards 01/02/03" anti-pattern below applies to **feature/service card grids**, where the number is decorative filler on items with no inherent order. It does NOT apply to `CoreValuesSection`'s "How we decide what to build" 01–06. Indices render `text-(--color-border-strong)` (ghost) at the [Numerals] treatment, going `--color-accent` on `group-hover`, with NO rule above them — the cell is just `<article className="group">`.
- History — do not re-litigate: user asked for these indices on 2026-07-31, they were removed later the same day as part of a minimal pass, and the user immediately asked for the grid back ("no bring it back i dont want this layout please") and confirmed the numbered version by picking it from three options. **The numbers stay.** Don't propose removing them again.
- Where: allowed in `components/sections/CoreValuesSection.tsx`; still banned in `app/services/ai-automations/AIAutomationsClient.tsx`.
- Date: 2026-07-31

### [Section] — surface rhythm: alternate grounds, white slabs only
- Rule: A long page must not run every section on `--color-bg`. Alternate two grounds so no two adjacent sections read as the same slab: default `--color-bg` (transparent section, no bg class) ↔ `--color-surface` white slab. Give a white slab to the page's densest text block so it lifts off the warm ground either side.
- Padding exception: an anchor slab (the page's densest block) uses `py-24 md:py-32`, NOT the standard `py-16 md:py-20`. Alongside the already-documented hero/contact-form exceptions, this is the only body-section deviation — transparent sections and ordinary white-surface sections keep `py-16 md:py-20`.
- **Do NOT use a dark/inverted slab** (`bg-(--color-text) text-(--color-bg)`) as the mid-page anchor. Tried on About's `CoreValuesSection` 2026-07-31 — user rejected it: "not huge fan of dark background all of a sudden in between". A dark band mid-scroll reads as a lurch, not a beat. Don't re-propose it; use `--color-surface` and wider padding instead. (This also removes the need for `--color-bg`-as-ink overrides — light-theme tokens apply normally: `text-(--color-text)`, `text-(--color-text-muted)`, `border-(--color-border)`.)
- Where: About page — `CoreValuesSection` is the anchor slab (`bg-(--color-surface) py-24 md:py-32`); `StatsCounters` restored to its default `bg-(--color-surface)` (the `bgClassName=""` override in `app/about/page.tsx` was removed, so About now matches home). Sequence: bg → bg → WHITE → bg → WHITE → bg → bg → bg.
- Why: user flagged the About page as "lot of text to read" with "no room to breathe" — every section was the same padding on the same ground, so the page had one flat texture and nothing to rest against. Ground contrast does the breathing that more padding alone can't; white-on-warm-off-white is enough contrast to do it without a tonal jolt.
- Date: 2026-07-31

### [Accent] — page budget: one static yellow, hover states only after that
- Rule: A page gets ONE static `--color-accent` moment. Everything else that wants yellow gets it on hover or not at all. Never colour a *set* of repeated elements (6 principle indices, 3 step circles) in accent — that's 3–6 yellow instances in one section and blows both accent rules ("max once per section", "more than ~3 on a page is too much").
- Where: About page — the single static yellow is the hero's "we build systems". The Insights accent circles were removed; the `CoreValuesSection` 01–06 indices render ghost (`--color-border-strong`) and only reach accent on `group-hover`, one at a time, so they cost nothing against the static budget.
- Exception (user-requested 2026-07-31): `VisionSection`'s H2 word "bottleneck" is a second static accent word on the About page. Budget for a *long* page is therefore **one accent word per screenful, max ~2–3 static per page** — both instances are single words in display/H2 type, far apart in the scroll, and never a repeated set. Do not read this as a licence to accent a third heading.
- Date: 2026-07-31

### [Copy] — density budget: a section carries one idea, not one paragraph per idea
- Rule: On content pages, cap supporting copy hard — section sub-line ≤ 12 words, card/principle description ≤ 14 words, hero lede ≤ 32 words, CTA subheading ≤ 25 words. When a sentence lists outcomes ("response time, conversion, capacity, cost"), pull the list OUT of the prose and set it as type — a hairline-bordered `grid-cols-2 md:grid-cols-4` row, one term per cell at `text-h3 font-bold`. Words-as-design-elements is the brand rule; it also cuts reading time to a glance.
- Where: About page pass — hero lede 48w→30w, `VisionSection` two dense paragraphs → lede + 4-term measurables row + one `text-h3 font-normal` closing line, `CoreValuesSection` descriptions ~22w→≤14w, `PhotoGallerySection` 22w subhead → a 4-word meta line right-aligned beside the H2, `InsightsSection` descriptions ~30w→~13w and subhead 20w→6w, `StatsCounters` closing note 22w→11w, About CTA subheading 44w→21w. Page body copy dropped ~45% with the story arc (Tension → Shift → Resolution → Invitation) and section order untouched.
- Why: user asked to keep intent and flow but said there was "a lot of text to read". Cutting words, not sections, is what preserves the arc.
- Update (2026-07-31): `VisionSection`'s 4-term measurables row and its `text-h3` closing line were later cut entirely (user-requested) — the section is now heading + 2 short paragraphs left, one `aspect-video` photo right. The list-out-of-prose pattern itself still stands; it just isn't used here anymore.
- Date: 2026-07-31

### [Layout] — asymmetric heading/body split instead of an equal two-column text block
- Rule: For a heading + supporting prose section, don't run `grid lg:grid-cols-2 gap-12` with a paragraph in each half — two equal text columns read as twice the reading work. Use `grid lg:grid-cols-12`: heading in `lg:col-span-5`, body in `lg:col-span-6 lg:col-start-7`. The skipped column IS the whitespace — no extra padding needed.
- Where: `components/sections/VisionSection.tsx`.
- Date: 2026-07-31

### [Component] — ONE case-study section, shared by home and /work
- Rule: The case-study block (bordered wrapper → featured 2-col card → 2-up grid) exists exactly once, as `components/sections/FeaturedWorkSection.tsx`, and reads its content from `lib/work-data.ts` (`FEATURED_CASE_STUDY` + `CASE_STUDY_GRID`). It renders the card primitives from `components/sections/work/CaseStudyCard.tsx` (`CaseStudyFeaturedLink`, `CaseStudyGridLink`). Only the copy above it varies, via optional `heading` / `subheading` / `id` / `ariaLabel` props defaulting to the home page's wording; `/work` passes `heading="Case studies"`.
- **Do not build a page-local variant of a block that already exists on another page.** The deleted `WorkCaseStudiesSection.tsx` was a second implementation of this exact layout, and `FeaturedWorkSection` carried its own hardcoded `FEATURED` / `CASE_STUDIES` arrays — so the same three case studies existed as two copies of the copy, already drifting (home had `tags` uppercased into the banned eyebrow style, and all three cards linked to `/work` instead of the case study's own slug).
- Where: `components/sections/FeaturedWorkSection.tsx`; consumed by `app/page.tsx` and `app/work/WorkPageContent.tsx`.
- Why: user — "you should not add/create new component here, you should reuse the component used in home page only." Generalises the existing [Reuse] rule from motion primitives to whole sections: if a page needs a block another page already has, parameterise that block, don't fork it.
- Date: 2026-08-01

### [Rejected] — do NOT collapse ANY FAQ accordion via grid-template-rows: 0fr→1fr
- Rule: **Never use a `grid-template-rows` transition to collapse `.accordion-content`/`.accordion-content-seo`, on either accordion, ever.** `fr` units aren't reliably interpolatable across browsers — a transition on them frequently just snaps instantly with no visible animation, AND can fail to collapse fully, leaving one line of the answer visible under its closed question. This has been tried and reverted **three times** on this exact class: introduced 84bf8d2, reverted 040da0e (2026-08-01, user: "the collapsed row didn't fully flatten"), reintroduced by a teammate at 9978276 believing it now "collapsed fully clean" — it hadn't; user hit the identical bug again 2026-08-04 ("removed entire animation. why the answer row 1 is visible on the collapsed faqs"), fixed the same day by moving to a height-based approach (below). If someone proposes grid-fr again, point at this entry — it does not work here, three tries is enough data.
- Current implementation (2026-08-04) — **two separate classes, because the two accordion consumers have different Radix Content lifecycles and each needs its own collapse mechanism**:
  - `.accordion-content` (`app/faq/FaqPageContent.tsx`) — Radix's default mount/unmount `Content`, **no** `forceMount`. Collapses via the `accordion-down`/`accordion-up` **keyframe `animation`** (not a `transition`) in `app/globals.css`, because Radix's `Presence` only detects a CSS *animation* to time when it's safe to unmount after closing — a `transition` here would make Presence unmount the node instantly, killing the close animation entirely.
  - `.accordion-content-seo` (`components/sections/FaqSection.tsx`, `forceMount`) — every answer stays mounted (open or closed) for SEO, so it needs the opposite: a `transition: height` against Radix's own measured `--radix-accordion-content-height`, **not** a keyframe `animation`. A `transition` only fires on a `data-state` *change*, never on initial mount — critical here, since every closed item matches `[data-state="closed"]` from first paint; a keyframe `animation` on that selector would replay a "closing" animation for every already-closed FAQ on page load.
  - Don't merge these back into one shared class — that's what caused the confusion that let the grid-fr regression slip back in twice.
- Kept: `defaultValue={items[0]?.id}` on `FaqSection`'s `AccordionPrimitive.Root`, so the **first question renders open** and every other one starts closed (user-requested, 2026-08-01).
- Date: 2026-08-01, updated 2026-08-04

### [SEO] — FAQPage schema comes from one helper, once per route
- Rule: `faqPageJsonLd(items)` in `lib/faq-data.ts` builds the schema.org `FAQPage` object; never hand-write the literal. `FaqSection` emits it automatically for any page that renders that component, so **a page using `FaqSection` must not also declare its own** — two `FAQPage` blocks on one URL is invalid. `/faq` is the exception that needs its own call (bespoke layout, doesn't use `FaqSection`) and makes it from inside `FaqPageContent.tsx`, not `page.tsx`.
- Trap: an audit that greps only `app/**/page.tsx` for `ld+json` will miss schema declared in a co-located `*Content.tsx` client component and wrongly report the page as having none — grep the whole route folder.
- Where: `lib/faq-data.ts`, `components/sections/FaqSection.tsx`, `app/faq/FaqPageContent.tsx`.
- Date: 2026-08-01

### [SEO] — alt text describes the image, not the thing it stands in for
- Rule: When a card uses generic/stock art as a placeholder for a real asset, the alt is `alt=""` (decorative), not the project or client name. `alt="Tocal"` on `/our-services/webdevelopment.webp` describes something not in the frame — and that same file backed two different projects. The name is already in the adjacent `<h3>`, so an empty alt is both accurate and correct a11y. Put a real alt back when real screenshots land.
- A logo IS the company, so the logo branch of `CaseStudyVisual` keeps `alt={company}`; only the illustration branch goes empty.
- Where: `components/sections/work/CaseStudyCard.tsx` (`CaseStudyVisual`, `ExternalProjectLink`).
- Date: 2026-08-01

### [SEO] — declared OG dimensions must match the actual file
- Rule: `openGraph.images[].width/height` is a claim about the file — check it. `public/og-image.png` was 1672x941 while 21 files declared `1200x630`. Now 1200x675 (16:9, resized not cropped, 1.37 MB → 210 KB via `sharp` `png({ palette: true, quality: 90, effort: 10 })`) with every declaration updated to match. Keep the `.png` filename on re-encode so previously-shared links don't break.
- Don't blanket sed `height: 630` — `app/opengraph-image.tsx` is Next's runtime `ImageResponse` generator whose own `size` is legitimately 1200x630, and `app/work/[slug]/page.tsx` picks `study.image ?? '/og-image.png'`, where the two branches have genuinely different dimensions (1448x1086 vs 1200x675) and need a per-branch object.
- Date: 2026-08-01

### [Perf] — canvas ambient loops must idle out, not spin forever
- Rule: A decorative canvas driven by pointer input has to stop its `requestAnimationFrame` chain once there's nothing left to animate, and restart on the next input. `DotGrid` draws the frame, then `if (painted.size === 0) { idle = true; return }`; `wake()` restarts the chain and is called from `handleMouseMove` and from the resize handler (resizing a canvas clears it, so it needs one repaint at the new size).
- Where: `components/ui/DotGrid.tsx` — sitewide, `<DotGrid global />` is on most pages.
- Why: the loop redrew every dot every frame forever. On a 390×844 phone that's ~1,600 `ctx.arc` calls at 60fps — permanently — while `mousemove` never fires on touch, so it could never paint anything. Pure battery and main-thread cost with zero visible output. Mouse users see no difference: the static grid is drawn, then the loop parks until the cursor moves.
- Date: 2026-08-01

### [Anti-pattern] — never render invented client names as proof
- Rule: Placeholder brand names in a "who we've built for" marquee are fabricated social proof — never ship them. `ClientLogosSection`'s `CLIENTS` array now holds real client logo files only (`public/client-logos/*`), no invented names.
- Placement: live on `/` (`app/page.tsx`) and `/about` (`app/about/page.tsx`). Deliberately **not** on `/work` — that page's premise is "Proof over promises" via named testimonials (`WorkTestimonialsSection`), and a logo marquee there would be redundant within the same scroll.
- Where: `components/sections/ClientLogosSection.tsx`.
- Date: 2026-08-10

### [Rejected] — do NOT restyle /work in the About page's visual language
- Rule: `/work`'s existing blocks are approved as they are. A pass on 2026-08-01 rebuilt them using the About page's devices — hero proof band, `--color-surface` anchor slabs alternating with `--color-bg`, an editorial ruled-type index replacing the "Selected websites" card grid, big metric numerals on the case-study cards. **User rejected all of it wholesale**: "this is poor all AI slop nothing is good. you used the same elements like about page. i dont want it, whatever it was previously it's very nice." Reverted in full.
- Follow-ons the user then stated directly: don't touch the `/work` hero copy; **no section backgrounds on this page unless the section genuinely needs one** (`StatsCounters` is passed `bgClassName=""` on `/work` for this reason, unlike home where it keeps its white surface). Section-level reuse across pages is wanted; new page-local components are not.
- Why: worth keeping because the reasoning behind that pass wasn't wrong on the facts (the stock-image reuse and the invented client names were real problems) — the failure was applying another page's finished visual system to a page that already had its own working one. Fix the specific defect; don't restyle the page around it.
- Date: 2026-08-01

## Naming
_None logged yet._

## Anti-Patterns (Do NOT Do)
### [Motion] — never observe the moving element for a masked reveal
- Rule: For a masked slide-up (`overflow-hidden` clip + inner `translateY(110%)`), put `whileInView`/IntersectionObserver on the STATIC clip wrapper, not on the translated inner element.
- Why: IntersectionObserver measures the *transformed* bounding box. The inner line starts shifted 110% down, so its observed rect is wrong and the reveal never fires (symptom: headings stay invisible). The wrapper doesn't move → reliable trigger; it drives the inner via variants.
- Where: `RevealText` in `components/ui/Reveal.tsx`.
- Date: 2026-06-28

### [Minimal] — no decorative hairline above every item in a grid
- Rule: Default to NO rule/marker above each cell of a card or step grid — whitespace and type hierarchy do the separating (design-system.md: *"No decorative dividers — whitespace creates separation"*). Also drop small marker glyphs/circles used as bullets. If an item needs a marker to feel designed, the layout is the problem.
- Where: removed 2026-07-31 from `InsightsSection` (per-item `border-t`, and the accent-bordered numbered circles), `VisionSection` (the measurables row's `border-t`/`border-b`), `AboutHeroSection` (proof band `border-t`).
- Also removed from `CoreValuesSection` — its per-item `border-t` (and the `pt-6` that spaced content off it) is gone. The `01`–`06` indices there stay (user-chosen, see [Exception] below): the *digits* were wanted, the separator line above them was not. Worth remembering as the distinction — an index label can earn its place; a rule stamped above every cell does not.
- Why: user reviewed the rendered About page — "I still believe there is tons of AI slop… change it to minimal style, don't add unnecessary markers like separators on top of something."
- Date: 2026-07-31

### [Minimal] — images are square; radius cap is 4px, no exceptions
- Rule: Photos and image cards get NO border-radius and no border — `relative h-full w-full overflow-hidden bg-(--color-bg-muted)` is the whole treatment. design-system.md caps container radius at 4px; `rounded-2xl` (16px) violates it and reads soft/generic against the site's hard editorial type. Don't add a hover radius-morph (`transition-[border-radius] … hover:rounded-none`) either — it's an interaction the scroll reveal already covers.
- Where: `components/sections/PhotoGallerySection.tsx`.
- Date: 2026-07-31

### [Motion] — all magic happens on scroll; hover stays functional only
- Rule: Entrance/reveal choreography is scroll-triggered via the `Reveal.tsx` primitives, staggered by index. Hover is reserved for things that are actually interactive (links, buttons, cards you can click) and stays subtle — colour or underline, per motion-system.md. Don't invent hover flourishes on static content (colour-shifting numerals, radius morphs on non-clickable images) to make a section feel "interactive"; it isn't interactive, and the flourish reads as decoration.
- Where: About page — removed an image radius-morph on non-clickable photos; kept the staggered `RevealText`/`RevealFade`/`RevealImage` scroll reveals. `CoreValuesSection`'s numeral `group-hover` accent stays (user-chosen, see [Exception] above).
- Why: user — "keep it interactive but don't add unnecessary interactions. all magic should happen on scroll."
- Date: 2026-07-31

### [Responsive] — never ship two `hidden`-toggled copies of the same content
- Rule: Don't build a "desktop layout" in `hidden md:block` and a "mobile layout" in `md:hidden` rendering the same items. Every word ends up in the DOM twice — duplicate content for crawlers, double the Reveal components mounting and observing, and two layouts to keep in sync. Build one layout that responds (`grid gap-10 md:grid-cols-3 md:gap-8`). Only duplicate markup when the two versions are genuinely different *components*, never when they're the same data in a different flow direction.
- Where: `components/sections/InsightsSection.tsx` — had a horizontal desktop timeline and a vertical mobile stack, both mapping the same `INSIGHTS` array. Collapsed to one grid.
- Date: 2026-07-31

### [Grid] — spans must sum to the column count at every breakpoint; no nested flex to fill a gap
- Rule: In a `col-span`/`row-span` image grid, pick spans so each row sums exactly to the column count at BOTH breakpoints (4 on desktop, 2 on mobile). If a cell has to split itself with an inner `flex` to fill leftover width, the span math is wrong — fix the spans, don't nest. A nested `w-2/3` + `w-1/3` split inside a `col-span-2` cell collapses the 1/3 child to a ~55px sliver on a phone.
- Mobile row height should be viewport-relative (`auto-rows-[42vw]`) so a 1×1 cell stays roughly square at any phone width; switch to fixed rows once the grid goes 4-up (`md:auto-rows-[200px] lg:auto-rows-[230px]`). A fixed `auto-rows-[140px]` at 2 columns gives wide-letterbox cells on small phones and near-square on large ones — inconsistent crop across devices.
- Where: `components/sections/PhotoGallerySection.tsx` — 7 photos with an `id === 4` nested-flex special case became 8 flat entries; the `photo.id === 4` branch is gone. Layout: `[1:2×2][2][3] / [1][4][5] / [6:2×1][7][8]`.
- Date: 2026-07-31

### [Motion] — case-study + external-project card images now use RevealImage
- Rule: `CaseStudyVisual` (`CaseStudyCard.tsx`, feeds `FeaturedWorkSection` on home + `/work`) and `ExternalProjectLink` (`WorkWebsitesSection` grid) had headings/copy/card entrance on `RevealText`/`RevealFade` already, but the card thumbnail was a plain `<Image>` — no clip-wipe. Wrapped both in `RevealImage` (`className="relative h-full w-full"` inside the existing `overflow-hidden` container) so every image on the page reveals the same way as About's gallery/hero images. Hover-scale transform on `ExternalProjectLink`'s image stays on the `<Image>` itself, independent of the wrapping reveal.
- Where: `components/sections/work/CaseStudyCard.tsx`.
- Why: user asked for uniform text/image/card reveal on `/work` matching other pages; images were the one primitive not yet applied there (and on home, since the component is shared).
- Date: 2026-08-01

### [Page] — author landing page: single-column author-first layout + infinite-scroll article list
- Rule: `/blog/author/[authorSlug]` is scoped to the author, not a mini blog index — everything (identity block, bio, article list) sits in one `max-w-[720px] mx-auto` column, same content width as the post detail page's body (`app/blog/[slug]/page.tsx`), not the wide `container` grid other blog landing pages (`category`, `label`, `tags`) use. No `FeaturedBlogHero` / `selectFeaturedPost` on this page — author pages render every article in the same one-by-one list, none singled out as "featured".
- Header order: square author image (`h-24 w-24 md:h-28 md:w-28`, `object-cover`, no radius/border per [Minimal] images-are-square rule below — NOT the old `rounded-full` circle) next to name (`h1`, `text-h2`) + designation directly under it, then an optional LinkedIn link, then `shortBio` (short description, `text-lg`), then full `bio` (`PortableTextContent`), then a `border-t` divider, then an `h2` "Articles" heading, then the article list.
- Author schema gained a new optional `designation` field (`sanity/schemaTypes/authorType.ts`) — "what they do", e.g. "Content Strategist". Distinct from `shortBio` (the longer one-line intro/SEO description) and from `bio` (full portable-text body). Frontend fallback when empty: `"Author at Website Vikreta"` (`FALLBACK_DESIGNATION` const in the page) — never render a blank designation line.
- Articles render via `components/blog/AuthorArticlesList.tsx` (client component) as a 2-col grid of `BlogCard`s (`grid grid-cols-1 sm:grid-cols-2 gap-x-8 gap-y-16`) capped at 2 columns (not the `lg:grid-cols-3` other listing pages step up to) since the page is constrained to the narrower 720px column. Lazy-loads: initial + each subsequent batch is 6 posts on desktop (`≥768px`, matches Tailwind `md:`) / 4 on mobile, sliced client-side from the full `fetchPostsByAuthor` result (no separate paginated query — author post counts are small). An `IntersectionObserver` on a sentinel div (`rootMargin: '400px 0px'`, fires before the user hits bottom) grows `visibleCount` by one page size at a time; `window.matchMedia('(min-width: 768px)')` (+ `change` listener, so a resize/rotate mid-session re-picks the page size) decides which page size to use. Once every post is visible, the sentinel unmounts and an end-of-list message renders: "You've reached the end — that's every article from this author."
- Where: `app/blog/author/[authorSlug]/page.tsx`, `components/blog/AuthorArticlesList.tsx`, `sanity/schemaTypes/authorType.ts`, `sanity/types/index.ts` (`Author.designation`), `sanity/lib/queries.ts` (`AUTHOR_BY_SLUG_QUERY` now selects `designation`).
- Why: user spec — page should focus on the author only (no featured post, no other blog-index chrome), constrained to the blog content column width, with the identity fields in a specific order and infinite-scroll article loading at specific breakpoint counts.
- Date: 2026-08-02
- Update (2026-08-02): outer container/breadcrumb compacted to `/blog`'s rhythm — `container pt-20 pb-16 md:pt-24 md:pb-20`, breadcrumb `mb-6 md:mb-8` (was the more generous `pt-32.../mb-10 md:mb-14` other landing pages use) — and internal section gaps stepped down to the matching 6/8/10/12 scale. LinkedIn is now a real CTA, not a `TextLink`: pill button (`rounded-full border border-(--color-border) bg-(--color-surface) px-4 py-2 text-sm font-medium`, hover `border-(--color-text) bg-(--color-bg-muted)`) with an inline-SVG LinkedIn glyph (`LinkedinIcon` in the page file — lucide-react has no brand/logo icons, so this is a one-off hand-drawn glyph, not a new dependency) + "View on LinkedIn" label. `shortBio` gets a quiet card treatment (`border border-(--color-border) bg-(--color-bg-muted) p-5 md:p-6`) to separate it from the page's plain-text sections. `bio` gets a small `h2` label ("Bio", `text-lg font-bold tracking-tight`) directly above it — deliberately smaller than the "Articles" `h2` (`text-h3`) so the two don't compete, per [Hierarchy] intentional H2 step-down (same tag, different visual weight is fine).

### [Hero] — /blog index hero is a label-driven slider, heading sits below it
- Rule: `/blog`'s hero is `components/blog/FeaturedBlogHeroCarousel.tsx` (client), fed every post carrying the Sanity label slug `featured-articles` (title "Featured Articles" — no label literally titled "Featured Blog" exists in the schema; user confirmed reusing this one over creating a new label). Fetched via `fetchPostsByLabel(HERO_LABEL_SLUG, HERO_LIMIT)` in `app/blog/page.tsx`. With 0 tagged posts it falls back to the old single-post `selectFeaturedPost` pick so the hero never renders empty; with exactly 1 post it renders the plain `FeaturedBlogHero` card (no carousel chrome for a single slide); 2+ posts get the same card markup switching via `AnimatePresence mode="wait"` (opacity-only cross-fade, `REVEAL_EASE`, `AUTO_INTERVAL = 7000`ms, pauses on hover, `useReducedMotion` stops autoplay), plus a dot row + `ChevronLeft/Right` arrow pair below the card (`CarouselButton`, same bordered-square style as `WorkTestimonialsSection`'s `CarouselButton`). `BlogPageHeading` ("Blogs") now renders directly below the hero slider (previously above it, outside the Suspense boundary) — it moved inside `BlogResults` so it stays in one Suspense boundary with the hero; `BlogResultsSkeleton` grew a heading placeholder line between the hero and carousel-row skeletons to keep this CLS-safe.
- `fetchPostsByLabel`'s third param generalized from `excludeSlug?: string` to `excludeSlugs?: string | string[]` (also threaded through `FeaturedLabelCarousel`/`FeaturedLabelCarousels`), since the hero can now exclude multiple slugs from the label-carousel rows and grid below it, not just one.
- Where: `app/blog/page.tsx`, `components/blog/FeaturedBlogHeroCarousel.tsx`, `components/blog/BlogResultsSkeleton.tsx`, `sanity/lib/fetch.ts`, `components/blog/FeaturedLabelCarousel.tsx`, `components/blog/FeaturedLabelCarousels.tsx`.
- Date: 2026-08-02

### [Rejected] — blog post title hover is NOT accent; every title is still a link
- Rule: Post title hover tried `hover:text-(--color-accent)` on 2026-08-02, **reverted same day** — user asked to put it back to what it was previously. Current state: `FeaturedBlogHero.tsx` / `FeaturedBlogHeroCarousel.tsx` title link → `hover:text-(--color-text-muted)` (their original treatment); `BlogCard.tsx` grid-card title → same `hover:text-(--color-text-muted)`; `FeaturedLabelCarousel.tsx`'s `CarouselCard` title → no hover color at all (its original state — the card's `group/img` hover only scales the image). **Don't re-propose accent on title hover.** The one part of that pass that stayed: `BlogCard.tsx`'s `<h2>` title is still wrapped in a `postHref(post.categorySlug, post.slug)` `Link` (previously plain unlinked text) — that was a separate "add a link" request, not the color change, and wasn't reverted.
- Also fixed in passing while touching these titles (still stands): `FeaturedBlogHero.tsx`'s "Read more" `TextLink` had pointed at the pre-migration `/blog/${post.slug}` — corrected to `postHref(post.categorySlug, post.slug)` per [Reuse] `lib/blog-url.ts`.
- Where: `components/blog/BlogCard.tsx`, `components/blog/FeaturedBlogHero.tsx`, `components/blog/FeaturedBlogHeroCarousel.tsx`, `components/blog/FeaturedLabelCarousel.tsx`.
- Date: 2026-08-02

### [Hero] — fixed-height hero card via em-based line-clamp, skeleton matched to it
- Rule: `FeaturedBlogHero.tsx` / `FeaturedBlogHeroCarousel.tsx`'s title and description no longer size to their own content — each gets an explicit `h-[Nem]` wrapper sized to its own font-size/line-height (title: `h-[3.3em]` = 3 lines at `leading-[1.1]`, `line-clamp-3` — bumped from an initial 2-line/`h-[2.2em]` pass after user feedback that 2 lines truncated too eagerly; description: `h-[5.1em]` = 3 lines at `leading-[1.7]`, `line-clamp-3`) so overflow text truncates with `…` instead of growing the box. Because the em unit is relative to the *same element's* font-size, the height class must sit on an element carrying the matching text-size classes (title: `text-3xl sm:text-4xl`, description: `text-base`) — putting the height on a differently-sized ancestor/descendant would resolve to the wrong pixel value at each breakpoint. This makes total hero card height constant across every slide (a 1-line title and a 3-line title occupy the same box), so `AnimatePresence` slide-switching and the arrow/dot nav below never jump.
- `BlogResultsSkeleton.tsx`'s hero placeholder was rebuilt to mirror this exactly — its title/description filler blocks carry the identical `h-[3.3em] text-3xl sm:text-4xl leading-[1.1]` / `h-[5.1em] text-base leading-[1.7]` wrapper classes (with a solid `bg-(--color-bg-muted)` fill inside), so the skeleton's height matches the real hero's height precisely and swapping skeleton → real content causes no layout shift.
- Where: `components/blog/FeaturedBlogHero.tsx`, `components/blog/FeaturedBlogHeroCarousel.tsx`, `components/blog/BlogResultsSkeleton.tsx`.
- Why: user — with the hero now a multi-post carousel (see [Hero] label-driven slider entry above), each slide's post has a different title/description length, so on the old shrink-to-fit layout the hero box changed height every time it auto-advanced or the skeleton resolved into real content.
- Date: 2026-08-02

### [Component] — Breadcrumb `hoverColor` variant + compact header now sitewide across /blog/*
- Rule: `components/ui/Breadcrumb.tsx` has an optional `hoverColor?: 'muted' | 'accent'` prop (default `'muted'`, the pre-existing `hover:text-(--color-text)` behavior everywhere else) — a component-param opt-in, not a fork, per the existing [Reuse] convention.
- Header container padding `pt-20 pb-16 md:pt-24 md:pb-20` is now the value for **every `/blog/*` landing page** — `/blog`, `/blog/search`, `/blog/categories/[slug]`, `/blog/labels/[slug]`, `/blog/tags/[slug]`, `/blog/authors/[authorSlug]` all use it. **This supersedes the 2026-08-02 entry** that called it a page-scoped `/blog`-only divergence and warned against propagating it — user explicitly asked for identical top spacing across every blog route, so the landing pages that still carried the sitewide `pt-32 pb-20 md:pt-40 md:pb-28` were brought down to match. `/careers` was left untouched (out of scope, not a blog route). The post detail page (`/blog/[slug]/[postSlug]`) keeps its own `pt-20 pb-6 md:pt-24 md:pb-8` — same top, shorter bottom because a cover image follows immediately; this is the one page excluded from the rest of this entry by explicit user instruction.
- On the four taxonomy landing pages (`categories`/`labels`/`tags`/`authors`), the plain `Breadcrumb` call was later swapped for `BlogHeaderBar` (breadcrumb + inline search box, see [Component] entry below) — same padding, same `hoverColor="accent"`, just wrapped in the shared header component instead of calling `Breadcrumb` directly.
- Where: `components/ui/Breadcrumb.tsx`, `app/blog/page.tsx`, `app/blog/categories/[categorySlug]/page.tsx`, `app/blog/labels/[labelSlug]/page.tsx`, `app/blog/tags/[slug]/page.tsx`, `app/blog/authors/[authorSlug]/page.tsx`.
- Date: 2026-08-02, superseded 2026-08-03 (see [Breadcrumb] / [Nav] entries below for the same date).

### [Nav] — breadcrumb segments must include the taxonomy root, matching the URL
- Rule: Any `/blog/{taxonomy}/{slug}` landing page's breadcrumb must have ONE segment per URL path step — `Home / Blog / {Taxonomy plural} / {item}` — not skip straight from `Blog` to the item. Segment hrefs point at the real folder route, and every taxonomy folder is plural: Category → `{ label: 'Categories', href: '/blog/categories' }`, Tags → `{ label: 'Tags', href: '/blog/tags' }`, Label → `{ label: 'Labels', href: '/blog/labels' }`, Author → `{ label: 'Authors', href: '/blog/authors' }`. Only the final segment (the item itself) has no `href`.
- Where: done in all four — `app/blog/categories/[categorySlug]/page.tsx`, `app/blog/tags/[slug]/page.tsx`, `app/blog/labels/[labelSlug]/page.tsx`, `app/blog/authors/[authorSlug]/page.tsx`.
- Why: breadcrumb previously read `Home / Blog / {item}`, silently skipping the `/categories`, `/tags`, `/labels`, `/authors` URL segment — hierarchy didn't match the actual path, and `BreadcrumbList` structured data (if added later) would misreport the site tree.
- Date: 2026-08-03

### [Page] — taxonomy index page (`/blog/categories`, `/blog/tags`, `/blog/labels`, `/blog/authors`)
- Rule: The base route for each taxonomy (URL with the slug dropped) is a real Server Component, not a 404. Header block matches the existing `[slug]` landing pages exactly: `container pt-32 pb-20 md:pt-40 md:pb-28`, `Breadcrumb` (`Home / Blog / {Taxonomy}`, last segment no href), `h1` at `text-h2 font-bold leading-[1.1] tracking-tight` wrapped in `RevealText`. Grid uses the logged [Card] bordered-grid pattern: `grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 border-t border-l border-(--color-border)`, each cell `RevealFade` (staggered `delay={i * 0.05}`) with `border-r border-b border-(--color-border)`, a single `<Link>` inside (`bg-(--color-surface) hover:bg-(--color-bg-muted)`, focus-visible outline) — title `text-2xl font-bold`, description `text-sm text-(--color-text-muted) line-clamp-2`, post count as plain sentence-case `text-sm text-(--color-text-faint)` (never an uppercase/tracked eyebrow — see [Anti-pattern] no eyebrow labels). Author cards are the one variant: square `h-16 w-16` image (no radius, per [Minimal] images-are-square) + name + designation + post count, no description, `FALLBACK_DESIGNATION = 'Author at Website Vikreta'` when a designation is blank (mirrors the `[authorSlug]` detail page's fallback).
- Data: every index page has its own dedicated GROQ query with an inline `"postCount": count(*[_type=="post" && ... references(^._id)])` field, NOT the existing "with posts" queries (`fetchCategoriesWithPosts`, `fetchTagsWithPosts`, `fetchAllLabelsWithPosts`), which filter out empty taxonomy terms — the index page's whole job is to show every term, including ones with 0 posts. New queries: `ALL_CATEGORIES_QUERY` (extended, was pre-existing but unused elsewhere), `ALL_TAGS_QUERY`, `ALL_LABELS_QUERY`, `ALL_AUTHORS_QUERY` (all in `sanity/lib/queries.ts`) → `fetchAllCategories()`, `fetchAllTags()`, `fetchAllLabels()`, `fetchAllAuthors()` (all in `sanity/lib/fetch.ts`, same no-try/catch pattern — throws if Sanity isn't configured, matching `fetchAllPosts()`/`fetchAllCategories()`). `Category`/`Tag`/`Label`/`Author` types (`sanity/types/index.ts`) each gained an optional `postCount?: number`, populated only by these index queries.
- Where: `app/blog/categories/page.tsx`, `app/blog/tags/page.tsx`, `app/blog/labels/page.tsx`, `app/blog/authors/page.tsx` — all four built and build-verified (`npm run build` shows all four as static `○` routes).
- Date: 2026-08-03

### [Nav] — every taxonomy route folder is plural, no exceptions
- Rule: `app/blog/category` → `app/blog/categories`, `app/blog/label` → `app/blog/labels`, `app/blog/author` → `app/blog/authors` (all renamed, including their `[slug]` subfolders). `app/blog/tags` was already plural and untouched. **Every taxonomy folder under `/blog` is now plural** — don't reintroduce a singular one. This touched every `href`/canonical/`openGraph.url` string across the codebase that pointed at the old singular paths: the four `[slug]` pages' own metadata + breadcrumbs, the four index pages' metadata + card links, the post detail page's category/author links (`app/blog/[slug]/[postSlug]/page.tsx`), `FeaturedLabelCarousel.tsx`'s "View all" link, plus doc comments in `sanity/lib/fetch.ts`/`queries.ts`/`types/index.ts` and the Sanity Studio `canonicalUrl` helper text in `categoryType.ts`.
- Why: user — "do the label to labels category to categories and author to authors and tag to tags... avoid my spelling mistakes if any" (tags was already correct). Consistent plural naming across all four taxonomy routes, matching `tags`.
- Date: 2026-08-03

### [Page] — taxonomy `[slug]` post-listing pages: no featured hero, uniform lazy-loaded card grid
- Rule: The four taxonomy landing pages that list posts under a specific term (`/blog/categories/[categorySlug]`, `/blog/tags/[slug]`, `/blog/labels/[labelSlug]`, `/blog/authors/[authorSlug]`) render every post as the same `BlogCard` — no `FeaturedBlogHero`/`selectFeaturedPost` singling one post out as a hero/carousel. Card loading is scroll-triggered via the shared `components/blog/InfiniteBlogGrid.tsx` (`posts`, optional `gridClassName`, `endMessage` props): initial batch + each subsequent batch is 6 posts on desktop (`≥768px`) / 4 on mobile, sliced client-side from the full fetched post list, grown via an `IntersectionObserver` sentinel (`rootMargin: '400px 0px'`) so the next batch starts loading before the user hits the bottom. `gridClassName` defaults to the wide taxonomy-page 3-up (`sm:grid-cols-2 lg:grid-cols-3`); the author page (constrained to a 720px column, see below) passes `gridClassName="sm:grid-cols-2"`.
- `AuthorArticlesList.tsx` (the original single-page implementation of this pattern, see the [Page] "author landing page" entry below) was deleted and replaced by `InfiniteBlogGrid` — don't recreate a page-local infinite-scroll list; this is the one shared implementation for all four taxonomy pages now, per the site's [Reuse] convention.
- Where: `components/blog/InfiniteBlogGrid.tsx`; consumed by all four `[slug]` pages above. `/blog`'s own hero (`FeaturedBlogHeroCarousel`) is untouched — the "no featured hero" rule is scoped to the taxonomy landing pages, not the blog index.
- Why: user — "don't add any featured image or carousel in the following routes everything must be in the form of card and also add lazy loading... only cards visible in the screen should be loaded first then after scrolling below them next batch should be loaded." The author page already had this exact lazy-load pattern (just not generalized); category/tag/label previously used `selectFeaturedPost` + a single big hero card above a plain (non-paginated) grid.
- Date: 2026-08-03

### [Layout] — blog post-card grid gap: 1.25x bump (2026-08-04)
- Rule: all post-card grids (BlogCard in a `grid`) across `/blog` routes use `gap-x-10 gap-y-20 md:gap-x-[3.125rem] md:gap-y-[6.25rem]` — was `gap-x-8 gap-y-16 md:gap-x-10 md:gap-y-20`. Applies to the blog index (`BlogListingClient.tsx`), search results (`app/blog/search/page.tsx`), the shared `InfiniteBlogGrid.tsx` (all four taxonomy `[slug]` pages + author page), its loading skeleton (`BlogResultsSkeleton.tsx`), and the "Related reads" 2-col grid on the post detail page (`app/blog/[slug]/[postSlug]/page.tsx`, `gap-x-8 gap-y-16` → `gap-x-10 gap-y-20`, no `md:` variant there since it's already capped at `sm:grid-cols-2`) — the skeleton must mirror the real grid's gap or the loading→loaded swap shifts layout.
- The same 1.25x ratio extends to the blog-home label carousel rows (`FeaturedLabelCarousel.tsx` + `LabelCarouselSkeleton.tsx`): row gap `gap-6` (1.5rem) → `gap-[1.875rem]`. The hero slider (`FeaturedBlogHeroCarousel.tsx`) is untouched — it swaps one full-width card, no inter-card gap exists there.
- Scope: does NOT cover the bordered link-tile grids on `/blog/{categories,tags,labels,authors}` index pages (`gap-6`, border-t/border-l divider pattern) — those are tiles, not `BlogCard`s, and weren't part of this request.
- Why: user — "all the cards looks too compact in the grid... increase spacing between grid altogether in the entire /blog routes. i want to 1.25x what it is currently," then "do the same for blog home sliders also" and "do same change in related post on blog detail page as well." 8→10 and 16→20 land on Tailwind's curated scale exactly; the md-grid values (10→12.5, 20→25) and the carousel gap (6→7.5) don't, so those use arbitrary bracket values (`3.125rem`, `6.25rem`, `1.875rem`) for precision instead of rounding.
- Date: 2026-08-04

### [Component] — label-carousel controls: overlay left/right arrows, tracked edge states
- Rule: `components/blog/ScrollableRow.tsx` (client) wraps a snap-scrolling card row and overlays prev/next arrows on it — absolutely positioned at `left-2`/`right-2`, `top-1/2 -translate-y-1/2`, not in a bar below. Same square `CarouselButton` shape as the hero's (h-9 w-9, border, invert-on-hover) plus `bg-(--color-surface)` + `shadow-sm` so it reads over a card image, and `disabled:opacity-30`.
- It tracks real scroll position via a `scroll` listener + `ResizeObserver` (not just click state, so a manual swipe updates it too): the whole arrow pair is hidden when `scrollWidth <= clientWidth` (nothing to scroll), left disables at `scrollLeft ≈ 0`, right disables at the end of `scrollWidth`. `FeaturedLabelCarousel.tsx` (Server Component, fetches its own posts) passes `BlogCard`s in as `children` — data fetching stays server-side, only the scroll/button interaction is client-side. `LabelCarouselSkeleton.tsx` needs no arrow placeholder — the overlay is `absolute`, so it never reserves layout space either loading or loaded.
- **`FeaturedBlogHeroCarousel.tsx` is explicitly out of scope for this pattern** — user tried the same overlay treatment there, then said "the slider control for the hero should be like before only." Reverted in full: hero arrows stay in the `mt-4 flex justify-between` bar below the card, next to the dot indicators, exactly as originally shipped. Don't re-apply `ScrollableRow`-style overlay arrows to the hero without being asked again.
- Why: user — "add controls on sliders as well," then "blog controls should be on the left and right of the sliders. handle all edge cases, like disable when to show when to not" (scoped down to label rows only after the hero revert).
- Date: 2026-08-04

### [Page] — /blog index: "All Blogs" grid caps at 6, category pills are plain links (no in-place filter)
- Rule: `app/blog/page.tsx`'s `getPosts()` fetches `GRID_LIMIT + HERO_LIMIT` (6 + 6 = 12) raw posts via `fetchFilteredBlogPosts({ limit: 12 })`, not just 6 — `BlogResults()` excludes whichever of those are already shown as the hero, then `.slice(0, GRID_LIMIT)`. Fetching exactly 6 and excluding the hero afterward under-counts whenever a hero post is also among the newest 6 (the normal case, since hero posts tend to be recent) — that's the bug this fixes: user reported "All Blogs only fetched 4, not 6" after a first pass that fetched exactly `BLOG_INDEX_DEFAULT_LIMIT` (6, in `sanity/lib/fetch.ts`) with no headroom for the exclude.
- `BlogListingClient.tsx`'s category pills used to be `<button>`s holding client-side `activeCategory` state, filtering the same posts array in place ("isotope"-style, no navigation). **Removed entirely** — pills are now plain `<Link href={buildBlogSearchHref({ category: slug })}>` to `/blog/search`, and the component dropped `'use client'` (no more hooks, so no reason to ship it as a client bundle). The grid always shows the unfiltered top-6 preview; category browsing now always lands on `/blog/search` — consistent with the `[Page] — /blog/search: URL is the single source of truth` rule below, which already banned client-side filtering everywhere else on the blog.
- Why: user — "All Blogs only fetched 4. not 6" (undercounting fix), then "when i click on any category instead of isotope, remove that functionality completely. it should be redirect to search page with category filter selected."
- Date: 2026-08-04
- **Follow-up 2026-08-04**: converting the pills from stateful `<button>`s to plain `<Link>`s also dropped their only "active" visual — the selected/pressed pill's solid fill (`bg-(--color-text) text-(--color-bg)`) — since there's no active state left to fill for. User: "removed entire animation. bring it back" (a message that first got misread as being about the FAQ accordion regression fixed the same day — it wasn't; user corrected: "it wasnt about faq. it's for all blog category pills"). Fix: restore the fill as a **hover** effect instead of an active-state one — `hover:bg-(--color-text) hover:text-(--color-bg)` added alongside the existing `hover:border-(--color-text)` and `transition-colors duration-200 ease-out` (which was never actually removed — only the fill was). This is the only category-pill markup in the codebase (`grep buildBlogSearchHref({ category` → one hit); no other pill instance to fix in sync.
- **Correction 2026-08-04**: the conversion to plain `<Link>`s also silently dropped the leading **"All" pill** entirely (it used to be `handleCategoryClick(null)`, which has no equivalent once there's no client-side filter state) — user flagged this as the actual unintended regression, with a screenshot showing the pill row starting straight at the first real category. Restored: a static `<span aria-current="true">All</span>`, first in the row, styled with the same permanent solid fill the active pill used to get (`border-(--color-text) bg-(--color-text) text-(--color-bg) font-medium`) — **not** a link, no `href`, no hover state. It's always correct because the grid below is never filtered, so "All" is always the true current state of this page; there's nothing for it to navigate to.

### [Page] — taxonomy index + per-slug pages: "View all blogs" CTA → `/blog/search`, top-right + bottom-center
- Rule: all 8 taxonomy pages carry two identical CTAs, both pointing at `BLOG_SEARCH_PATH` (`/blog/search`, from `@/lib/blog-search-params`) — **not** `/blog`:
  - top-right, in the heading row: `<div className="mb-10 flex items-end justify-between gap-4 md:mb-14">` — heading on the left, `<TextLink href={BLOG_SEARCH_PATH} arrow="right" className="shrink-0">View all blogs</TextLink>` on the right.
  - bottom-center, after the post/tile grid (both the has-results and the empty-state branch): `<div className="mt-14 flex justify-center md:mt-20"><Button href={BLOG_SEARCH_PATH} variant="ghost" size="md" showArrow>View all blogs</Button></div>` — same markup as `BlogListingClient.tsx`'s pre-existing bottom "View all" CTA, which was always `/blog/search`.
  - Index pages (`app/blog/{tags,categories,labels,authors}/page.tsx`): heading is `RevealText h1`.
  - Per-slug pages (`app/blog/tags/[slug]`, `.../categories/[categorySlug]`, `.../labels/[labelSlug]`): the `h1` + optional description block became `<div className="flex items-end justify-between gap-4">{h1}{TextLink}</div>` followed by the description paragraph (kept outside the flex row, full width, unaffected by the row's `items-end`).
  - Author per-slug page (`.../authors/[authorSlug]`) is the one exception to "next to the h1" — that page opens with an image/name identity block, not a plain h1 heading, so `TextLink` sits next to the existing "Articles" `h2` above the post grid instead; bottom `Button` sits inside that same `border-t` "Articles" block, after the grid.
- Why: user — "on tags/ category/ label/ author/ all route should have CTA in center bottom to view all blogs. and a same cta should be there on right side of the heading of the page view all," then "make same for /tag/slug /author/slug /category/slug /label/slug as well," then **"view all blog should take to the search page. for all this recente canges in this session"** — first pass had all 16 CTAs (8 pages × 2) pointing at `/blog` (bare index), corrected to `/blog/search` across every one.
- Date: 2026-08-04

### [Card] — never number cards 01/02/03
- Rule: Don't add an index label (`01`, `02`, `03` …) to cards in a feature/service grid. No exceptions for "just a small counter in the corner."
- Why: user explicitly banned this after seeing it on the AI-automations "Three Things We Fix Most Often" grid — reads as generic template filler, not considered design. Let the image, title, and grid position carry the ordering instead.
- Where: `app/services/ai-automations/AIAutomationsClient.tsx` "Three Things We Fix Most Often" section (index field removed from `FixCard`).
- Date: 2026-07-04

### [Page] — /blog/search: URL is the single source of truth, zero client-side filtering
- Rule: `/blog/search` is the one place to browse/filter posts. Every filter lives in the query string and every query string is served by a fresh GROQ query — **no client-side filtering, ever**. Contract keys: `query` (free text; the literal value `all` is a no-op "everything" sentinel), `category` (one slug), `tags` (comma-separated slugs), `label` (comma-separated slugs; `labels` accepted as an alias), `sort` (`publish-date` | `title`), `sortOrder` (`asc` | `desc`), `page` (1-based). Page size 12.
- Both directions go through `lib/blog-search-params.ts` — `parseBlogSearchParams()` reads a URL, `buildBlogSearchHref()` writes one. **Never hand-build a `/blog/search?…` string.** `buildBlogSearchHref()` omits defaults and fixes key order, so one filter state maps to exactly one canonical URL (`?query=all&page=1` ≡ bare `/blog/search`).
- Parsing is total: junk degrades to a default, never a 500 or 404. `page=abc|0|-3` → 1 (capped at 1000); unknown `sort`/`sortOrder` → default; facet values sanitised to `[a-z0-9-]`, deduped, capped at 20; free text stripped of GROQ `match` wildcards (`*`), capped at 100 chars. A page number **past the end** 404s (dead URL, stops crawler walks); page 1 with no matches renders the empty state.
- Query: `buildBlogSearchQuery(sort, sortOrder)` in `sanity/lib/queries.ts` interpolates ONE filter expression into both `count()` and the `[$start...$end]` slice, so the total can't drift from the rows. Facets AND together; values within a facet OR. GROQ `order()` can't be parameterised, so the clause is *selected* from a whitelist table — never concatenated from user input. The GROQ param is `$searchQuery`, **not `$query`** — `query` is reserved on Sanity's `QueryParams` type and won't typecheck.
- Indexing policy (deliberate, see the comment block in `app/blog/search/page.tsx`): bare `/blog/search` + its `?page=N` walk are `index, follow`; **any filtered or re-sorted permutation is `noindex, follow`** — those are near-duplicates of `/blog`, `/blog/category/…`, `/blog/label/…`, `/blog/tags/…`, which are the pages we actually want ranked. `hasActiveFilters()` is the single predicate driving both that decision and the "Clear filters" control. Only the unfiltered URL is in `app/sitemap.ts`.
- UI: `BlogSearchFilters.tsx` is the only client component — it holds no filter state, just the in-flight text input (debounced 400ms, `router.replace` so keystrokes don't stack history; facet changes use `router.push`; Enter/the search-icon click submits immediately and cancels any pending debounce timer so a submitted query is never double-fetched). `useTransition` dims the still-mounted results while the next page streams; `useOptimistic` keeps a select/checkbox on its new value instead of snapping back. The input is re-synced from the URL **during render** (`if (syncedQuery !== params.query)`), not in an effect — the effect version both trips `react-hooks/set-state-in-effect` and lets a stale debounce undo an incoming URL. The debounce effect itself only schedules when `text.trim() !== params.query`, so URL-driven syncs never self-trigger a redundant fetch. **Any filter change resets `page` to 1.**
- Update (2026-08-04): the debounce described above had been dropped at some point in favor of submit-only (Enter/icon-click), with this entry left stale. Restored on user request ("allow user to search by entering any text") — type-to-search needed to actually search on typing, not just on Enter, while keeping request volume in check (an un-debounced query-per-keystroke would multiply Sanity reads and dim/rerender the grid mid-word). 400ms + the trim-diff guard + `router.replace` + the existing CDN-cached, 60s-revalidated GROQ query in `sanity/lib/queries.ts`/`fetch.ts` together keep this fast without adding load: repeats of an identical in-flight-window query (e.g. backspacing to a prior string) hit Next's fetch cache instead of re-querying Sanity.
- Pagination is server-rendered real `<a href>`s (`BlogSearchPagination.tsx`), never a "Load more" button — crawlable, bookmarkable, shareable.
- Entry points: **every "View all" on the blog goes to `/blog/search` with its constraint pre-applied** (label carousel rows → `?label=<slug>`; the index's bottom "View all" → bare). The `/blog` index header also carries a plain `<form role="search" method="get" action="/blog/search">` beside the breadcrumb — native GET, no client component, and the typed term lands pre-filled in the search page's input for free. `/blog/search`'s `h1` swaps to `Search results for “{query}”`, and the result-count line is `role="status"` so client-side filter navigations are announced (the `h1` alone updates silently).
- Legacy `/search` is now a `permanentRedirect` (308) into `/blog/search`, carrying and normalising any query string. `SearchClient.tsx` / `actions.ts` / `constants.ts` and `PAGINATED_POSTS_QUERY` / `fetchPaginatedPosts` were deleted with it — do not resurrect the load-more pool.
- Check: `lib/blog-search-params.check.ts` (assert-based, no framework). Run it after touching the parser — the command is in its header comment.
- Where: `lib/blog-search-params.ts`, `app/blog/search/`, `sanity/lib/queries.ts`, `sanity/lib/fetch.ts`, `app/blog/page.tsx`, `app/blog/BlogListingClient.tsx`, `components/blog/FeaturedLabelCarousel.tsx`, `app/search/page.tsx`, `app/sitemap.ts`.
- Date: 2026-08-03

### [Page] — blog post detail page's title is the page's `<h1>`
- Rule: `app/blog/[slug]/[postSlug]/page.tsx`'s post title is an `<h1>` (`text-h2` sizing kept as-is — only the tag changed). It was a bare `<h2>` with no `<h1>` anywhere else on the page — every blog post, the site's primary content/SEO surface, had zero `<h1>` elements. "Related reads" and any other sub-heading on the page stay `<h2>`, nesting correctly under it now.
- Where: `app/blog/[slug]/[postSlug]/page.tsx`.
- Why: found during a full `/blog/*` audit (routing/UI/perf pass); violated the existing `[Page] page-level heading hierarchy` rule above. Fixed on user request.
- Date: 2026-08-04

### [Card] — BlogCard title is `<h3>`, not `<h2>`, and its font-size is a class not an inline style
- Rule: `components/blog/BlogCard.tsx`'s title is `<h3 className="text-[clamp(1.05rem,1.6vw,1.35rem)] font-bold leading-[1.2] tracking-tight">`. It was `<h2>` with the identical clamp value as an inline `style={{ fontSize }}`. BlogCard is reused everywhere on the blog (`/blog` grid, `/blog/search` grid, every `FeaturedLabelCarousel` row, all 4 taxonomy `[slug]` landing pages via `InfiniteBlogGrid`, and post-detail "Related reads") — as `<h2>` it produced dozens of same-level headings per page with no `<h3>` tier, and the inline style violated code-standards.md's "no inline styles except dynamic values."
- Deliberately did NOT resize to the sitewide card-title H3 tier (`text-2xl sm:text-3xl`, see design-system.md/this file's `[Card] — bordered-grid listing card` entry) — that's for sparser cards (openings, case studies). BlogCard packs image + title + description + tags + footer in a dense 3-up grid; the bigger tier would crowd the rest out. The clamp value is intentionally its own smaller step, just moved from `style` to a Tailwind arbitrary-value class so it's no longer an inline style — not yet promoted to a named `--text-*` token since nothing else uses this exact size. If a second component ever needs it, extract a token then.
- Where: `components/blog/BlogCard.tsx`.
- Why: found during the same `/blog/*` audit; violated both `[Page] page-level heading hierarchy` (card titles must be `<h3>`) and code-standards.md's inline-style rule. Fixed on user request.
- Date: 2026-08-04

### [Anti-pattern] — no scroll-reveal animations anywhere under /blog/*
- Rule: **`/blog/*` renders content visible. No `whileInView`, no `RevealText`/`RevealFade`, no staggered entrance, no `opacity: 0` in the server HTML.** The only loading affordance on a blog route is a shimmer skeleton (`BlogResultsSkeleton`, `LabelCarouselSkeleton`) behind a Suspense boundary. This is a hard exception to the sitewide "revealing animations" non-negotiable in `CLAUDE.md` — marketing pages still reveal; the blog is a *reading* surface and does not.
- Why (user call, after a filtered `/blog/search` showed "2 articles" over an empty grid): an IntersectionObserver-gated `opacity: 0` costs LCP on the hero, blocks text from being readable until a scroll event fires, hurts accessibility, and — on a grid that swaps content in place from a client-side filter navigation — can strand freshly-arrived results permanently invisible. Loading state belongs in a skeleton, never in hiding real content.
- Consequences of the removal, all deliberate:
  - `BlogCard`, `FeaturedBlogHero`, `BlogPageHeading` dropped `'use client'` and are **Server Components** now. Less JS on every blog route, and the H1/LCP hero paint with the shell.
  - `BlogCard`'s `index` prop is **gone** (it only fed the stagger delay). Don't reintroduce it — use `key={post.slug}`.
  - `FeaturedBlogHeroCarousel` keeps its state/autoplay but imports **nothing from `motion/react`**; `useReducedMotion` was replaced by a local `usePrefersReducedMotion()` built on `useSyncExternalStore` + `matchMedia`, so no blog route pulls the animation library in for one media query.
  - Its slide swap re-renders **one persistent `<article>`**. The old `AnimatePresence mode="wait"` crossfade unmounted the slide for the length of the exit animation, collapsing the card to zero height — a real CLS bug. The em-based fixed heights on the hero title/description keep every post the same size, so the swap is shift-free.
- `BlogResultsSkeleton`'s hero must include the carousel's dot/arrow control row (`mt-4` + `h-9` = 52px). Without it the skeleton is 52px shorter than the real hero and the page jumps when content streams in. **Any change to a blog component's height must be mirrored in its skeleton** — that's the whole contract of the shimmer approach.
- Still outstanding, knowingly: the sitewide **Footer** still reveals (`opacity:0` + `translateY(60px)`) on every route including `/blog/*`. It's shared with every other page, so removing it is a sitewide decision, not a blog one. It sits below the fold and doesn't affect LCP/FCP.
- Where: `components/blog/BlogCard.tsx`, `FeaturedBlogHero.tsx`, `FeaturedBlogHeroCarousel.tsx`, `BlogPageHeading.tsx`, `BlogResultsSkeleton.tsx`, `app/blog/search/page.tsx`, and the eight `<BlogCard>` call sites.
- Date: 2026-08-03

### [Breadcrumb] — /blog/* breadcrumbs mirror the exact URL path; a segment with no real page is disabled, not linked
- Rule: every `/blog/*` breadcrumb segment must correspond to a real, resolvable route. Where the URL has an intermediate path segment that has **no landing page of its own** — `/blog/category`, `/blog/label`, `/blog/tags`, `/blog/author` all 404 bare — that segment still appears in the breadcrumb (so the trail reads as the literal URL: `Home / Blog / Category / {category.title}`) but carries **no `href`**, so `Breadcrumb.tsx`'s existing "no href → plain, non-clickable span" rendering disables it automatically. Don't invent a real target for it and don't drop it from the trail.
- Applies to: `app/blog/category/[categorySlug]/page.tsx` ("Category"), `app/blog/label/[labelSlug]/page.tsx` ("Label"), `app/blog/tags/[slug]/page.tsx` ("Tags"), `app/blog/author/[authorSlug]/page.tsx` ("Author"). `/blog` and `/blog/search` need no disabled segment — every segment in their trail is a real page. The post detail page's breadcrumb (`Home / Blog / {categoryTitle} / {post.title}`) intentionally links its category segment to `/blog/category/{slug}` rather than mirroring the post's own URL shape (`/blog/{categorySlug}/{postSlug}`) — that was accepted as-is, pre-existing behavior, not touched by this rule.
- Rule: **every `/blog/*` landing page except `/blog/search` and the post detail page** renders breadcrumb + a compact search box on the same line, via the one shared `components/blog/BlogHeaderBar.tsx` (`<BlogHeaderBar segments={...} />`, replaces a direct `<Breadcrumb ... />` call). Never hand-roll the breadcrumb+search row again — one component is what keeps five routes' top spacing and breadcrumb start position from drifting apart. The search box is a plain `<form role="search" method="get" action={BLOG_SEARCH_PATH}>` — no client component, no JS required, the browser builds `?query=…` itself and the term lands pre-filled in `/blog/search`'s own input.
- `/blog/search` is deliberately excluded from `BlogHeaderBar` — its own filter bar already has a search input directly below the breadcrumb; a second one beside it would be two search boxes stacked. Its breadcrumb is a plain `<Breadcrumb className="mb-6 md:mb-8" hoverColor="accent" />` call — **that className/hoverColor pair must be kept in sync with `BlogHeaderBar`'s own margins by hand**, since there's no shared component enforcing it on this one route.
- `BlogHeaderBar`'s row is `items-start`, not `items-center`, and stacks (`flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between`) below `sm:`. Both fixes for the same underlying bug (user-reported, 2026-08-03): `items-center` vertically centers the breadcrumb against the 40px-tall search input, pushing the breadcrumb text down by half the height difference — which put it out of alignment with `/blog/search`'s bare breadcrumb (no sibling to center against, so naturally flush-top). `items-start` pins both to the same top edge regardless of either one's height. Separately, the search box used to be `w-48 shrink-0` even on mobile — a fixed width that refuses to shrink squeezes a long category/tag/author name into a cramped leftover column on a phone; it's now full-width and stacks below the breadcrumb under `sm:`, only sitting beside it from `sm:` up where there's room. **If this component's structure changes again, re-verify `/blog/search`'s bare breadcrumb still lines up — it's the one route with no sibling forcing the same alignment.**
- Where: `components/blog/BlogHeaderBar.tsx`, the four landing pages above, `app/blog/page.tsx`, `app/blog/search/page.tsx`.
- Date: 2026-08-03

### [Perf] — /blog index caps its post query instead of fetching the whole blog
- Rule: `fetchFilteredBlogPosts()` (`sanity/lib/fetch.ts`) now takes an optional `limit`, defaulting to `BLOG_INDEX_DEFAULT_LIMIT = 24`, sliced server-side in the GROQ query itself (`FILTERED_POSTS_QUERY`'s `order(publishedAt desc) [0...$limit]`) — not fetched-then-sliced in JS, so Sanity only ever returns the capped count over the wire. `/blog`'s index (`app/blog/page.tsx`) is the only caller and doesn't pass an override, so it gets the default. The category pills in `BlogListingClient` still filter client-side, but now over a capped 24-post window rather than the entire blog.
- Trade-off, accepted: a category with more than ~24 posts (after hero/label-carousel exclusion, often fewer) won't show its oldest posts under its pill on `/blog` itself. Not a regression in practice — `/blog` already has a "View all" button to the fully server-paginated `/blog/search`, and each category also has its own complete, uncapped landing page at `/blog/categories/[slug]`. The index was never the canonical "browse everything" surface; `/blog/search` and the taxonomy landing pages are.
- Still outstanding, knowingly: `fetchPostsByCategory`/`fetchPostsByTag`/`fetchPostsByAuthor` (powering `/blog/categories/[slug]`, `/blog/tags/[slug]`, `/blog/authors/[slug]`) remain fully unbounded — `InfiniteBlogGrid` only *renders* progressively, the full matching post list is still fetched up front every request. `fetchPostsByLabel` is the one already capped (`limit = 100` on the labels landing page). Fixing the other three needs `InfiniteBlogGrid` converted to fetch-on-scroll via a server action instead of slicing a fully-passed array — scoped as a separate pass, not done here.
- Where: `sanity/lib/queries.ts` (`FILTERED_POSTS_QUERY`), `sanity/lib/fetch.ts` (`fetchFilteredBlogPosts`), `app/blog/page.tsx`.
- Why: found during a full `/blog/*` UI/routing/perf audit — `/blog` fetched every post in the blog unconditionally on every request, with no cap, unlike `/blog/search` right next to it. Fixed on user request; the taxonomy-page fetches were explicitly scoped out to a later pass.
- Date: 2026-08-04

### [Hero] — featured hero card height is pinned to the image's own 16:9, via `aspect-[32/9]` on the row
- Rule: `FeaturedBlogHero.tsx` / `FeaturedBlogHeroCarousel.tsx`'s image is `aspect-video lg:aspect-auto` (back to the pre-2026-08-04 pattern), but now the `lg:grid lg:grid-cols-2` **row itself** carries `lg:aspect-[32/9]` with `lg:items-stretch`. `32/9` is exactly `2 × 16/9` — since the image sits in a half-width column, forcing the *whole row* to `32:9` makes the row's height equal precisely what a 16:9 image would be at that column's width, at every viewport, without computing it by hand. The image then stretches (`lg:aspect-auto` + `fill`) to exactly fill that height — so it's simultaneously "always 16:9" and "the card is always the image's height," the two of today's asks, satisfied by the same rule instead of two competing ones.
- This replaces the earlier same-day attempt (`lg:items-center` + manually-tuned `lg:py-8` padding) that only approximately matched image and text height at one viewport width (1440px) and visibly gapped at others — the `aspect-[32/9]` approach is exact at every width by construction, no tuning needed.
- Text column (`flex flex-col justify-center overflow-hidden`) stretches to the same row height and centers its content inside; `overflow-hidden` is the safety net for the narrow end of the `lg:` range (~1024–1150px) where the row's 16:9-derived height can come in shorter than the text's fixed em-based content height (title+description+meta+CTA ≈ 296px vs. row height ≈ 253px at 1024px) — a real, accepted trade-off, not chased further, since most traffic is well above that width.
- `BlogResultsSkeleton.tsx`'s hero placeholder mirrors this exactly (`lg:aspect-[32/9] lg:items-stretch`, `aspect-video lg:aspect-auto` on the image block, `overflow-hidden` on the text block).
- Where: `components/blog/FeaturedBlogHero.tsx`, `components/blog/FeaturedBlogHeroCarousel.tsx`, `components/blog/BlogResultsSkeleton.tsx`.
- Date: 2026-08-04

### [Component] — UTM-tagged social share row
- Rule: `components/ui/SocialShare.tsx` (`<SocialShare path title campaign />`) is the one social-share component — LinkedIn/X/Facebook/WhatsApp icon buttons (`react-bootstrap-icons`, matches `FooterSection`'s icon source) + a copy-link button (`lucide-react` `Copy`/`Check`, matches other lucide icon usage in the codebase). Every generated share/copy URL is tagged via an internal `withUtm()` helper: `utm_source=<platform>&utm_medium=social&utm_campaign=<campaign>`, built against `SITE_URL` + the page's own canonical path (never `window.location`, so the tag survives category-slug redirects etc.). `campaign` groups clicks per content type for GA4 (`"careers"` on `app/careers/[slug]/CareerDetailClient.tsx`, `"blog"` on `app/blog/[slug]/[postSlug]/page.tsx`). Each click also fires GA4's `share_click` via the new `trackShareClick(platform, campaign)` in `lib/analytics.ts` — same `sendGAEvent` pattern as `trackLinkClick`/`trackFormSubmit`, don't hand-roll a separate GA call.
- Where: `app/careers/[slug]/CareerDetailClient.tsx` (hero, under stipend/positions line), `app/blog/[slug]/[postSlug]/page.tsx` (byline block, under the `<hr>` — sanity-sourced posts only, matches the rest of the byline).
- Why: Jira WV-89 ("Add UTM parameters to social sharing links, career/internship posts especially") — no share buttons existed anywhere in the codebase before this, so the UTM requirement and the buttons themselves were built together. Reuse this component for any future share surface instead of hand-rolling new share links.
- Date: 2026-08-07

### [Perf] — above-fold hero text (label/subhead/CTA) fades in via pure CSS, never JS-gated, at the ORIGINAL GSAP timeline's exact durations/offsets
- Rule: `HeroSection.tsx`'s label, subheadline `<p>`, and CTA row use a shared `.hero-fade-in` class (`app/globals.css`, `@keyframes hero-fade-up`) instead of GSAP `opacity: 0 → 1`. Per-element timing is set via CSS custom properties inline — `--fade-y` (translateY start offset), `--fade-duration`, `--fade-delay` — same mechanism already used for `--char-i` in `Button.tsx`'s slot-char animation, don't invent a second stagger mechanism. Easing is `cubic-bezier(0.25, 0.46, 0.45, 0.94)` (power2.out approximation), not generic `ease-out` — chosen specifically to match the GSAP feel. `prefers-reduced-motion: reduce` is handled in CSS (`animation: none; opacity: 1;`), not JS.
- **The three elements' timing values are the original GSAP timeline's computed offsets, ported 1:1** — label `y:14px dur:0.5s delay:0s`, subhead `y:18px dur:0.65s delay:1.05s`, CTA `y:18px dur:0.6s delay:1.25s` (derived from the old `gsap.timeline()`'s `"-=0.2"/"-=0.3"/"-=0.45"` relative-position offsets against `0.5s/1.05s/0.65s/0.6s` durations). Do not flatten these to equal/arbitrary delays — user explicitly rejected a first pass that used flat `0s/0.15s/0.3s` delays as "compromising UI quality," even though it was still a valid perf fix. Match the original pacing exactly when porting a GSAP reveal to CSS; perf and "looks the same" are both required, not a trade-off to pick one of.
- The headline `<h1>` word-mask stagger (`.word-wrapper`/`.word-inner`, GSAP, gated behind `document.fonts.ready`) is untouched and stays GSAP — it's the one piece that must wait for real font metrics (mid-swap clipping risk), and it's the locked/signature motion per session.md.
- Why: PageSpeed Insights flagged the subhead `<p>` as the homepage LCP element with a 3.58s "render delay." Root cause: the old `[data-hero-anim] { opacity: 0 }` global rule (now deleted) hid it from first paint, and it only became visible after JS hydrated, `document.fonts.ready` resolved, AND its turn came up in the chained GSAP timeline. A CSS `animation` runs the instant the element paints, independent of JS/fonts — even with the delay values restored (subhead becomes visible ~1.7s after paint vs. the old 3.58s+), it's still a large win and comfortably under the 2.5s "Good" LCP threshold, because CSS animation timing runs on the compositor thread and isn't subject to Lighthouse's main-thread CPU throttling the way the JS font-wait was.
- Date: 2026-08-07

### [Accent] — do NOT use accent as a background-highlight-box workaround for text contrast; plain `text-(--color-accent)` stays, contrast failure is an accepted trade-off
- Rule: `var(--color-accent)` (`#FFD600`) text on `--color-bg`/`--color-surface` (`#FAFAF7`/`#FFFFFF`) measures ~1.35:1 contrast — a real WCAG 4.5:1 failure, not fixable by darkening the accent slightly (even `--color-accent-dark: #F3CB00` only reaches ~1.5:1; passing 4.5:1 requires darkening to an olive/mustard that no longer reads as the brand's yellow). A first attempt fixed this by converting accent-colored text to dark text on a yellow highlight-box background (`bg-(--color-accent) text-(--color-text)`) — this passes WCAG and matches how accent is used everywhere else in the codebase (background/border only, never text `color`, confirmed by grepping `globals.css`) — **but the user explicitly rejected it** as a UI/visual compromise and asked for the original look back.
- **Decision (user-confirmed 2026-08-07): keep plain `text-(--color-accent)`/`color: var(--color-accent)` on accent words, accept the contrast failure.** `HeroSection.tsx`'s "think" and `ServicesBentoGrid.tsx`'s "We build systems." are back to their original styling. Do not re-propose the highlight-box treatment for these two instances without being asked again.
- If accessibility/contrast is raised again for these specific words, the real options are: (a) plain yellow text, WCAG fails (current, user's pick), (b) highlight-box, WCAG passes but changes the visual (previously rejected), (c) darken the accent enough to pass, which stops looking like the brand yellow. There is no fourth option that keeps the exact color AND passes contrast — this is a color-math constraint (luminance ratio), not a missed technique.
- Where: `HeroSection.tsx` (ACCENT_INDEX word), `ServicesBentoGrid.tsx` (h2 "We build systems." line). `AboutHeroSection.tsx` has the same underlying pattern (`text-(--color-accent)` on "we build systems") and was never touched by this — same open question applies there if it ever comes up.
- Date: 2026-08-07

### [Perf] — headline word-mask reveal: real text static from first paint, PURE-CSS `.word-mask` overlay (no GSAP, no JS, no `'use client'`) fakes the "rise up" motion
- Rule: `.word-inner` (the real text) renders fully static and visible from first paint — no transform, no ref, no JS. A sibling `.word-mask` (`position:absolute; inset:0; background-color: var(--color-bg)`, one per word) sits opaquely on top of each word at rest and retreats via a plain CSS `@keyframes word-mask-retreat { to { transform: translateY(-100%) } }` animation — `animation: 1.05s cubic-bezier(0.16, 1, 0.3, 1) both word-mask-retreat`, staggered per word via `animation-delay: calc(var(--word-i, 0) * 65ms)` (same `--word-i`-on-inline-style convention as `--char-i` in `Button.tsx` — set via `style={{ '--word-i': i } as React.CSSProperties}`, don't invent a second stagger mechanism). `prefers-reduced-motion` hides masks via CSS media query (`display:none`) — no JS anywhere in this component for the reveal.
- **This superseded an earlier GSAP-driven version** (JS `useEffect` + `gsap.context` + `document.fonts.ready.then()` animating `yPercent` on the masks) that was functionally correct but carried ~32 lines of now-unnecessary JS: since `.word-mask` is `position:absolute; inset:0` on `.word-wrapper`, it auto-resizes with its parent on any font-swap reflow — the font-wait that mattered for the OLD (pre-mask) version, where the REAL TEXT's own percentage-based transform could clip if computed against stale metrics, doesn't apply to the mask, which recomputes every frame like any CSS percentage/transform value. Cutting the JS also meant `HeroSection.tsx` no longer needs `'use client'` at all (nothing else in the file uses hooks/browser APIs — `Button`/`UpworkBadge` aren't client components either) — it's now a plain Server Component, net -41 lines across the two files. Found via a `ponytail-review` pass (2026-08-08) — don't reintroduce GSAP/`document.fonts.ready`/`'use client'` here without a concrete reason the CSS-only version can't handle (e.g. if the stagger/easing ever needs to be dynamic/computed at runtime, which it doesn't today).
- Verified safe against `DotGrid` (the mouse-paint canvas bg): it renders blank until the cursor moves, so a solid `--color-bg` mask never visibly mismatches a "dotted" background at load — check this assumption again if `DotGrid` is ever changed to a static always-visible pattern.
- Why (original problem): confirmed via a real `next build && next start` + local Lighthouse (mobile, simulated throttling) run that the subhead fix alone didn't fix LCP — it just relocated the same 2.1s "element render delay" onto the h1. The mask makes the real text unconditionally paint-eligible immediately; the animation is 100% cosmetic and can't gate LCP by construction, regardless of timing.
- Where: `components/sections/HeroSection.tsx`, `app/globals.css` (`.word-wrapper`, `.word-inner`, `.word-mask`, `.hero-fade-in`).
- Date: 2026-08-07, simplified 2026-08-08

### [Perf] — local Lighthouse verification method + a known ceiling that's NOT the hero's fault
- Rule: To verify a perf fix against this project, don't test `next dev` (unminified, HMR overhead — produced a meaningless score of 39 once, do not reuse that number). Run `npm run build && npx next start -p <port>`, then `npx lighthouse http://localhost:<port> --only-categories=performance,accessibility --form-factor=mobile --screenEmulation.mobile --throttling-method=simulate --output=json --output-path=<path> --chrome-flags="--headless=new --no-sandbox" --quiet`. Chrome-launcher throws an `EPERM` on its temp-dir cleanup on this Windows machine every run — harmless, the JSON report is already written before that crash; read the report file directly, don't judge success by the CLI's exit code/stderr.
- **Known, confirmed-real ceiling (2026-08-07 measurement): total page JS is ~1MB, ~6s of cumulative main-thread work during load** (`mainthread-work-breakdown`/`bootup-time` audits), which keeps LCP simulated-poor (~6s) even with both hero fixes in place and TBT/CLS/accessibility(-minus-contrast) all healthy. This is spread across GSAP, `motion/react` (used by `ServicesBentoGrid`, `AboutHeroSection`, testimonial carousel, etc.), the `DotGrid` canvas rAF loop, GTM, and general hydration cost of the many `'use client'` sections on the homepage — not attributable to any single component. **User explicitly scoped this out as a separate future task (2026-08-07) — do not attempt to fix it opportunistically inside an unrelated task; it needs its own scoped pass** (bundle audit, code-splitting, deferring non-critical hydration).
- Date: 2026-08-07

### [Component] — "Back" links use lucide-react `ArrowLeft`, not a unicode `←` glyph or a Bootstrap-icon `<i>` tag
- Rule: Every drill-down-to-parent "Back" link (`app/work/[slug]/CaseStudyDetailClient.tsx` "Back to work", `app/careers/[slug]/CareerDetailClient.tsx` "Back to careers", `components/ContactPageContent.tsx` history-aware "Back" button) renders `<ArrowLeft className="h-4 w-4" />` + label inside `inline-flex items-center gap-1.5`, not a plain `←` character prefix. `ArrowLeft` comes from `lucide-react` (already a project dependency, used elsewhere e.g. `ArrowUpRight` in the same files) — never add Bootstrap Icons (`bi bi-*` classes) or any other icon library for this.
- Why: user asked for an icon-based back arrow instead of the plain-text `←` these three links originally shipped with; lucide-react was already in use in two of the three files, so reuse over a new dependency.
- Where: `app/work/[slug]/CaseStudyDetailClient.tsx`, `app/careers/[slug]/CareerDetailClient.tsx`, `components/ContactPageContent.tsx`.
- Date: 2026-08-13
